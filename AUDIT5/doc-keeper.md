# AUDIT5 · doc-keeper — доки как живой контракт

> Лейн: свежесть и целостность документации после июльской волны.
> Репо `/home/asulimenko/Project/workspace/ZooLink`, ветка `backend`, HEAD `c44874c`.
> Диапазон волны: `f3bcf40..c44874c` (13 коммитов, из них 13 тронули `docs`/`docsRU`).
> Дата прогона: 2026-08-04. Ничего не чинил — только зафиксировал (граница задачи).

---

## 0. Чего этот лейн НЕ увидит (границы честно, до находок)

| Не покрыто | Почему |
|---|---|
| **Рендер mermaid** | `mmdc` в системе отсутствует (`which mmdc` → пусто, exit 1). Проверены только структура/счёт/грамматика строк. **«Диаграмма рендерится» — НЕ доказано ни для одной из 51 диаграммы.** |
| **Смысловая верность перевода** | Сверял структуру, идентификаторы, числа, счётчики. Говорит ли русский текст то же, что английский — **не проверял**; это лейн переводчика/предметника. |
| **Док ↔ работающая система** | Тесты не гонял, БД не трогал (граница задачи). Сверка идёт док↔док и док↔ТЕКСТ миграции. «Миграция говорит X» ≠ «в живой PG X». |
| **Архитектурная правота решений** | Правильно ли ADR-0036…0040 решают задачу — компетенция **architect**. Я проверяю, что решения записаны, пронумерованы, зеркалированы и не противоречат друг другу. |
| **Файлы вне диапазона волны** | Кроме сквозных проверок (список файлов всего дерева, ERD, леджер). Дрейф в нетронутых волной доках не искал. |
| **Код** | Только grep-уровень: присутствует ли событие/идентификатор. Корректность реализации — **reviewer-qa** / **backend-engineer**. |
| **Не-.md/.yaml** | Картинки, схемы вне `.mmd`, вложения — не сверялись. |

### Три ложных срабатывания в МОИХ ЖЕ замерах (поймал сам, до отчёта)

Закон «замер обязан мерить то, что объявляет» бьёт и по аудитору. Фиксирую, чтобы число ниже читалось как проверенное, а не как объявленное:

1. **Глоб `migrations/2026070*_003[5-9]*.sql` не захватил `0039`** (файл `20260710_0039_…` начинается на `2026071`). Дал ложное «NOT FOUND» для `trg_confirmed_sales_immutable`, `nominated_buyer_user_id`, `amount_minor` — то есть **ложное обвинение леджера во лжи**. Перепрогнал явным списком файлов: все три на месте.
2. **`grep -c "SHIPPED"` по RU-спеке 18** дал 0 против 4 у EN → ложный вывод «RU не получил отметки об отгрузке». В RU они переведены: **`ОТГРУЖЕНО-form`**. Зеркало цело (7↔7 упоминаний миграций 0039/0040).
3. **`grep -oE '^\*\*[^*]+\*\*'` по глоссарию** посчитал жирные слова в НАЧАЛЕ ТЕЛА определения за заголовки терминов → ложное «EN 88 / RU 90». Реально **88 ↔ 88**; два «лишних» — `**Производная, пересчитываемая** сводка…` и `**Исправимый** исход модерации…`, стилистика русских определений.

Антарая всех трёх: **бхранти-даршана** (замер мерил не то, что объявлял). Все три пойманы перепроверкой и исправлены — но если бы я сдал отчёт после первого прогона, в нём было бы три ложные находки, одна из них — обвинение чистого леджера.

---

## 1. Сводка — топ-находки

| # | Severity | Антарая | Файл | Образец |
|---|---|---|---|---|
| **F-1** | 🔴 высокая | `прамада` (знал закон зеркала — применил в 3-м коммите, пропустил в 1-м и 2-м) | `docsRU/03-architecture/data-model.md` | RU до сих пор: «В MVP это **ТОЛЬКО ФОРМА**: гейт AGENT выключен… заглушка `AgentServiceTokenAuthenticator` не в цепочке аутентификаторов и всегда возвращает `null`» — при том, что миграция 0038 добавила 4 колонки и живой `POST /v1/auth/agent/token` |
| **F-2** | 🔴 высокая | `прамада` (EN-правка контракта без RU в том же change-set) | `docsRU/03-architecture/api-contracts/listings-api.yaml` | У `POST /listings` **нет ответа `'429'` целиком** — 23 структурных пути, включая `Retry-After`, `X-RateLimit-Limit`, `X-RateLimit-Remaining` |
| **F-3** | 🟡 средняя | `прамада` (в том же коммите обновил спеку 07 и event-catalog, леджер — нет) | `ZooLink/CLAUDE.md`, строка 45 (`0037`) | Леджер утверждает `NO \`SavedSearch.Matched\` event` и `` `q` NOT evaluated (documented) `` — c44874c ввёл **и то, и другое** |
| **F-4** | 🟡 средняя | `прамада` (данные были на экране — правил §12 на «ОТГРУЖЕНО», статус пятью строками выше не тронул) | `docs/specs/18-reputation.md` + RU-зеркало | `status: "Draft (FORM-first, design-only — no code/schema/migration in this change)"` против собственного тела `**✅ SHIPPED-form (2026-07-10, migration 0039 / ADR-0038)**` |
| **F-5** | 🟡 средняя | `стьяна` (правило объявлено в чартере, механизма нет — ничего не срабатывает) | `docs/specs/glossary.md` + RU | Волна ADR-0038/0039 дала **6** терминов (`Confirmed Sale`, `Sale Anchor`, `Review`, `Review Direction`, `Reputation Aggregate`, `Double-blind release`); волна ADR-0036/0037 — **0**: `agent_capability_profiles`, `scoped-ability`, `deny-by-default`, `Sybil`, `proof-of-transaction` не определены нигде |
| **F-6** | 🟢 низкая | `бхранти-даршана` (принял идентификатор за прозу и перевёл) | `docsRU/specs/security/rbac-matrix.md`, `docsRU/specs/07-geo-search-service.md` | `` `owner_id == актор` ``, `` `seller_id == актор` ``, `` `payment_transactions.user_id == актор` `` (×3); `` `широта` ``/`` `долгота` `` (×2), `` `R = 6_371_000 м` ``, `` `±100 м` `` |
| **F-7** | 🟢 низкая | `самшая` (читатель не отличит потерю от намеренного выравнивания) | `docs/04-decisions/README.md` + RU | Список идёт `ADR-0022` → `ADR-0035`; файлов 0023–0034 нет, объяснения разрыва нет ни в README, ни в `ECOSYSTEM_ADR_PLAN.md` |
| **F-8** | 🟢 низкая | `стьяна` (поле есть, не обновляется — декоративное) | `docs/specs/README.md`, `docs/specs/18-reputation.md` (+RU) | `lastUpdated: "2026-05-28"` в индексе, куда строку 18 добавили 2026-07-09; у спеки 18 `lastUpdated: "2026-07-08"` при правках тела 2026-07-10 |

**Класс, а не три случая.** F-1, F-2 и F-3 — одна и та же поломка: **закон зеркала/леджера держится в `docs(...)`-коммитах и рвётся в `feat(...)`/`fix(...)`-коммитах, где доки правятся попутно к коду.** Замер по волне:

| Тип коммита | Тронули доки | EN/RU спарено | Расхождение |
|---|---|---|---|
| `docs(...)` — авторские | 6 | **6 / 6** | 0 |
| `feat(...)`/`fix(...)` — код+доки попутно | 7 | **5 / 7** | **2** |

Оба непарных — `594002a` (agent-auth) и `2457cf1` (confirmed_sales). Обоим не хватило **одного и того же файла**: `docsRU/03-architecture/data-model.md`. F-2 — четвёртый экземпляр того же класса, `a6efe56` от 2026-07-08 (до волны, тип `fix(...)`).

---

## 2. Проверка 1 — зеркальность EN↔RU по файлам волны

**Метод.** Список пород из git: `git log --name-only f3bcf40..c44874c -- docs docsRU | sort -u`. Для каждого EN — существование RU-пары, затем структурное сравнение: YAML — разбор PyYAML 6.0.2 и сравнение множества путей ключей с игнорированием прозаических узлов (`description`/`summary`/`title`/`example`); Markdown — последовательность уровней заголовков, число строк таблиц, мультимножество код-спанов, мультимножество чисел.

**Числа с нагрузкой:**

- **23 EN-файла** тронуты волной, **23 RU-файла** — тронуты. **Пар отсутствует: 0.**
- Сквозной список всего дерева: `docs` и `docsRU` расходятся **на 4 файла** — все **EN-only и все пред-волновые**, все четыре числятся в `EN_RU_CONSISTENCY_AUDIT.md` (строки 36–37, 59–64) как известный долг: `02-requirements/database-audit-report.md`, `02-requirements/priority1-completion-summary.md`, `localization/migration-summary.md`, `project-structure-map.md`. **Волной не порождены, новым дефектом не считаю.**
- **9 YAML-контрактов** сверены структурно: **8 чистых, 1 расходится** (F-2, 23 EN-only пути).
- **Дубли ключей во ВСЕХ 18 файлах контрактов (EN+RU): 0.** Починка `950a7c9` держится. Проверял загрузчиком с перехватом `construct_mapping` — `safe_load` дубли молча съедает, поэтому обычная сверка их не увидела бы.
- **14 Markdown-пар:** последовательность уровней заголовков совпадает **во всех 14** (например `data-model.md` — 47/47, ADR-0039 — 25/25, спека 18 — 26/26).
- **Битых относительных ссылок в 14 md-парах: 0.**
- **`ConfirmedSale.Created` в коде — ложная тревога:** три вхождения, все в комментарии и в тесте, который УТВЕРЖДАЕТ обратное (`…eventType === 'ConfirmedSale.Created')).toBe(false)`). Event-catalog («**reserved**, НЕ эмитится с якоря передачи») — **правдив**.

### F-1 (🔴) — `docsRU/data-model.md` пропустил две из трёх поставок

Единственный файл волны, отставший по содержанию. Точная бухгалтерия по коммитам:

| Коммит | Слайс | EN data-model | RU data-model |
|---|---|---|---|
| `594002a` | agent-auth, ADR-0036/0037, миг. 0038 | ✅ правлен | ❌ **не тронут** |
| `2457cf1` | confirmed_sales, ADR-0038, миг. 0039 | ✅ правлен | ❌ **не тронут** |
| `4fa63d9` | reputation storage, ADR-0039, миг. 0040 | ✅ правлен | ✅ правлен |

**Не долетело: 20 добавленных строк и 7 заменённых.** Измеримые следы в RU:

- упоминаний **миграции 0038 — 0** (EN: 2); **миграции 0039 — 0** (EN: 1);
- **`agent_capability_profiles` — 0** вхождений (EN: 2). Целого пункта «Agent capability profiles» в RU нет;
- **строки `confirmed_sales` в таблице операционных доменов нет** (RU идёт `digital_assets` → `reviews`, EN — `digital_assets` → `confirmed_sales` → `reviews`). Отсюда 21 строка таблицы у EN против 20 у RU;
- поверхность ADR-0036/0037 в RU не упомянута ни разу: `auth/agent/token`, `admin/agents`, `capability_profile` — **0** вхождений (EN: 4).

**Почему это тяжелее «устаревшей прозы».** RU не просто молчит — он **активно утверждает снятое положение дел**:

> «В MVP это **ТОЛЬКО ФОРМА**: гейт AGENT выключен, поэтому строка не создаётся и секрет не проверяется; заглушка `AgentServiceTokenAuthenticator` не в цепочке аутентификаторов и всегда возвращает `null`.»

EN на этом месте: `service_credentials` … «made **live** by ADR-0036 + ADR-0037, migration 0038», четыре новые колонки, и живой админский путь выдачи. Оба верно говорят, что **поведение под гейтом** (`feature_toggles.agent_service_auth` = off) — но RU отрицает существование формы, которая уже в схеме. Слабая модель, читающая RU-ветку, сделает вывод, что колонок нет.

### F-2 (🔴) — RU-контракт объявлений не знает про `429`

`POST /v1/listings` в EN отдаёт `'429'` (превышена квота создания, 20 за скользящие 24ч, AUDIT4 P1-4) с тремя заголовками. В RU этого ответа **нет**: EN несёт два блока `'429'` (строки 208 и 426), RU — один (403).

Происхождение: `a6efe56` (2026-07-08, `fix(consent,listing,ops)` — **до** волны, подтверждено `git merge-base --is-ancestor`), который правил `docs/03-architecture/api-contracts/listings-api.yaml` и не трогал RU. Волна дефект не внесла, но и **не поймала** — это ровно тот класс, который свип обязан ловить.

Расхождение машиночитаемое: потребитель RU-контракта не сгенерирует обработку 429 и не узнает про `Retry-After`.

### F-6 (🟢) — переведено то, что обязано быть дословным

Глоссарий сам объявляет конвенцию: *«code/contract identifiers (table/column names, ENUM values, role constants) are kept verbatim in both EN and RU; only prose is translated»*. Нарушения внутри код-спанов:

- `docsRU/specs/security/rbac-matrix.md` — `` `owner_id == актор` `` (117), `` `seller_id == актор` `` (125), `` `payment_transactions.user_id == актор` `` (128); в EN — `== actor`;
- `docsRU/specs/07-geo-search-service.md` — `` `широта` ``/`` `долгота` `` (103, 104), `` `R = 6_371_000 м` `` (137), `` `±100 м` `` (141).

Низкая: ни один из этих токенов не является реальным именем колонки (реальные — `listings.lat`/`lng`), поэтому в код это не протечёт. Но конвенция нарушена, и правило «сравни код-спаны» на этих файлах даёт вечный шум, маскируя настоящее расхождение.

---

## 3. Проверка 2 — леджер `ZooLink/CLAUDE.md`, строки 0035–0040

**Счётчики таблиц — правдивы.** Цепочка леджера `0034: +1 → 37` · `0035/0036/0037: +0 → 37` · `0038: +1 → 38` · `0039: +1 → 39` · `0040: +2 → 41`.

Сверка с истиной: `grep -cE "^CREATE TABLE" database_schema.sql` = **41**. Прирост подтверждён по телу миграций: `0038` создаёт `agent_capability_profiles` (1), `0039` — `confirmed_sales` (1), `0040` — `reviews` + `reputation_aggregates` (2). **Арифметика сходится, конечное число совпадает с источником истины.**

**Идентификаторы — 16 из 16 на месте** (после починки моего глоба, см. §0):

`content_updated_at`→0035 · `idx_consents_seq_current`→0036 · `saved_search_matched`→0037 · `uq_agent_capability_profiles_code`, `capability_profile_id`, `issued_by`→0038 · `trg_confirmed_sales_immutable`, `nominated_buyer_user_id`, `amount_minor`, `uq_confirmed_sales_transfer`→0039 · `trg_reviews_immutable`, `uq_reviews_current_per_direction`, `chk_consents_consent_type`, `REVIEW_PUBLICATION`, `rating_avg`, `superseded_by_id`→0040.

**Имена констрейнтов — 9 из 9 присутствуют в `database_schema.sql`** (гейт дрейфа схема↔миграции зелёный): `chk_confirmed_sales_status`, `chk_confirmed_sales_market`, `chk_confirmed_sales_anchor`, `uq_confirmed_sales_transfer`, `uq_reviews_current_per_direction`, `chk_consents_consent_type`, `uq_agent_capability_profiles_code`, `trg_reviews_immutable`, `trg_confirmed_sales_immutable`. Проверено и заявленное «`rating_avg` NUMERIC(3,2) GENERATED … STORED» + «PK (subject_user_id, market)» — в схеме дословно (строки 756, 764).

### F-3 (🟡) — строка `0037` леджера опровергнута коммитом `c44874c`

Два утверждения, ложные на HEAD:

| Леджер (строка 45) | Реальность на `c44874c` |
|---|---|
| `NO \`SavedSearch.Matched\` event` | Событие **эмитится**: `saved-search-match.consumer.ts:124`, `eventType: 'SavedSearch.Matched'`, `schemaVersion: 1` |
| `` `q` NOT evaluated (documented) `` | `q` **вычисляется**: спека 07 §SS-M2 — «**`q` free-text IS evaluated** server-side as a **case-insensitive substring** match» |

Тот же коммит **обновил спеку 07 и `event-catalog.md` в обеих ветках** (EN 2 / RU 2, спарено) — и не обновил леджер. Отсюда антарая `прамада`, а не `стьяна`: правило известно и в этом же change-set применено к двум документам из трёх.

Вес выше косметики: `ZooLink/CLAUDE.md` **автозагружается в контекст каждой сессии**. Модель, планирующая работу по гео-поиску, прочтёт «`q` не вычисляется, события нет» и построит на этом слайс. Это прямое попадание в закон №10 (писать для слабейшей модели — она **не догадается** сверить леджер со спекой).

Оговорка честности: если леджер задуман как **исторический** журнал «что сделал слайс 0037 на момент 0037», строки формально верны для своего момента. Но соседние строки того же файла говорят о текущем состоянии («41 таблица», «~two dozen invariants enforced»), заголовок называет его *living* — то есть жанр объявлен как «состояние», и читать его будут как состояние. **Решение о жанре — не моё**, вынесено в §7.

---

## 4. Проверка 3 — индексы ADR и §13 спеки 18

- **`0035`–`0040` присутствуют в индексе и EN, и RU — 6 из 6, статусы совпадают.**
- **Собственный `Status` каждого ADR — Accepted, 6 из 6 в обеих ветках:** EN `**Status**: Accepted (owner, 2026-07-09)`, RU `**Статус**: Accepted — принят (владелец, 2026-07-09)`. Даты сходятся (0035 — 2026-07-08, 0036–0040 — 2026-07-09).
- **Ни один Accepted не переписан задним числом**: правки волны — только добавление новых номеров; переходы Proposed→Accepted (`24dcace`) сделаны отдельным коммитом с датой решения владельца. `0020`/`0021` честно остались **Proposed** — расхождения индекса с телом нет.
- **Строки индекса зеркальны 1:1**, включая хвостовые скобки-аннотации и памятку `ECOSYSTEM_ADR_PLAN`.
- **§13 спеки 18 — отметка RESOLVED на месте в обеих ветках**: EN стр. 537 `## 13. Open Questions / Assumptions — RESOLVED by the owner (2026-07-09)`, RU стр. 549 `## 13. Открытые вопросы / Допущения — РЕШЕНЫ владельцем (2026-07-09)`. Все 8 развилок с нормативной пометкой «CONFIRMED exactly as recommended» / оговоркой по развилке 8 (юр. подтверждение) — присутствуют в обеих.

### F-7 (🟢) — разрыв нумерации 0023–0034 нигде не объяснён

Файлов `0023`…`0034` нет; индекс прыгает `0022` → `0035`. Скачок **выглядит** намеренным (номер ADR совпал с номером миграции: ADR-0035 ↔ миграция 0035, ADR-0038 ↔ 0039… уже не совпадает), но ни README, ни `ECOSYSTEM_ADR_PLAN.md` этого не заявляют — grep по `numbering|gap|skip|reserv|align` пуст. Читатель не может отличить «12 решений потеряно» от «номера зарезервированы намеренно». Механической починки нет: **что именно написать — знает architect**.

### F-4 (🟡) — спека 18 противоречит сама себе

В одном файле, в обеих ветках:

- шапка: `status: "Draft (FORM-first, design-only — no code/schema/migration in this change)"`, RU — `"Draft (FORM-first, только проектирование — без изменений кода/схемы/миграций)"`;
- хвост: «*Design-only. No `database_schema.sql`, migration, ERD, event-catalog, ADR, or code changed by this spec.*»;
- тело §12: «**✅ SHIPPED-form (2026-07-10, migration 0039 / ADR-0038)**» и «**✅ SHIPPED-form (2026-07-10, migration 0040 / ADR-0039)**» (RU — `ОТГРУЖЕНО-form`, 7 упоминаний миграций против 7 у EN).

Зеркало здесь **исправно** — обе ветки устарели одинаково. Дефект не в синхронизации, а в **свежести**: тело догнало реальность, самоописание — нет.

---

## 5. Проверка 4 — перекрёстные ссылки

**event-catalog ↔ spec 07 (последними правил `c44874c`) — сходятся.** Полезная нагрузка события сверена с кодом посимвольно:

| Источник | Поля |
|---|---|
| `event-catalog.md:57` | `savedSearchId, listingId, subjectUserId, market, matchedAt` |
| `saved-search-match.consumer.ts:124-131` | `savedSearchId, listingId, subjectUserId, market, matchedAt` |

Совпадает и семантика «ровно один раз на пару»: каталог — «Emitted **exactly once per (saved_search, listing) pair** — only when the alert row is newly inserted (SS-M4-consistent)», спека 07 §SS-M4 — то же условие. RU-каталог (строка 56) зеркален. **Единственный, кто отстал по этому событию, — леджер (F-3).**

**Индекс спек — 18 строк целы в обеих ветках.** EN строка 18 → `[18-reputation.md](18-reputation.md) - Reputation & confirmed-sale primitive (FORM-first)`; RU строка 32 → `[18-reputation.md](18-reputation.md) - Примитив репутации и подтверждённой сделки (FORM-first)`. Backfill 11–17 из `ed2580f` на месте с обеих сторон.

**Глоссарий ↔ спека 18 — термины покрыты:** `Confirmed Sale` (`confirmed_sales`), `Sale Anchor`, `Review` (`reviews`), `Review Direction`, `Reputation Aggregate` (`reputation_aggregates`), `Double-blind release` — 6 из 6, в обеих ветках. Всего терминов **88 ↔ 88** (см. §0, п.3 о ложном 88/90).

### F-5 (🟡) — глоссарий покрыл одну волну и не заметил вторую

Асимметрия по одной и той же июльской волне:

| Подволна | Новые понятия | Записей в глоссарии |
|---|---|---|
| ADR-0038/0039 (репутация) | confirmed sale, review, aggregate, double-blind | **6** |
| ADR-0036/0037 (agent-auth) | scoped-ability, capability profile, deny-by-default | **0** |

Проверено по обеим веткам, счёт одинаков — то есть **зеркало исправно, дыра симметрична**. Не определены нигде: `agent_capability_profiles` (EN 0 / RU 0), `service_credentials` (0/0), `AGENT JWT` (0/0), `deny-by-default` (0/0), `least-privilege` (0/0), `Sybil` (0/0, при том что «анти-Sybil» — нормативное понятие ADR-0040), `proof-of-transaction` (в EN встречается 2 раза **внутри чужих определений**, своей записи нет; в RU — 0).

Причина видна из истории: последняя правка глоссария — `950a7c9`, мой же предыдущий свип («glossary +7»), который шёл ПОСЛЕ репутационных ADR и ДО того, как кто-либо перечитал agent-auth. Коммит `594002a` глоссарий не открывал. Правило «сверься с глоссарием, прежде чем вводить термин» живёт **только в чартере** — ни гейта, ни линтера, ни шага в чек-листе слайса. Отсюда `стьяна`: объявление есть, действия нет.

---

## 6. Проверка 5 — mermaid и ERD

**`ZooLink_ERD.mmd` — 41 сущность, совпадает с 41 таблицей схемы ПОИМЁННО.**

- блоков сущностей: `^    [a-z_]+ \{$` → **41**; закрывашек `^    \}$` → **41** (баланс блоков сходится);
- сравнение множеств имён ERD ↔ `database_schema.sql`: **разность пуста в обе стороны** (`comm -23` и `comm -13` — по нулю строк). То есть не только счёт, но и состав;
- новые сущности волны определены и связаны: `confirmed_sales` (4 ребра), `reviews` (3), `reputation_aggregates` (1), `agent_capability_profiles` (1);
- рёбер-связей: **58**, ссылающихся на неопределённую сущность — **0**;
- построчная грамматика: строк вне класса «пусто / `%%`-комментарий / `erDiagram` / открытие сущности / закрытие / атрибут / связь» — **0**.

Про баланс скобок отдельно, чтобы число не соврало: сырой `grep -c '{'` даёт 90 против 48 `}` и выглядит как «UNBALANCED». Это **артефакт синтаксиса mermaid** — токены кардинальности (`}o--o{`, `||--o{`) содержат скобки. Правильный замер — по якорям начала/конца блока, он и дал 41/41.

**Паритет диаграмм EN↔RU:** ```` ```mermaid ````-блоков в `docs` — **25**, в `docsRU` — **25**; файлов с расхождением по числу блоков — **0**. Плюс один отдельный `.mmd` (канон ERD, вне дерева локализации) → **51 диаграмма** в репозитории. Заявленная в чартере база «39 диаграмм» с текущим состоянием **не совпадает** — счёт вырос; это, судя по всему, устаревшая база, а не пропажа, но проверить её происхождение в рамках лейна нечем.

> **Честно и главное:** `mmdc` отсутствует (`which mmdc` пуст). **Рендер не проверен ни для одной из 51 диаграммы.** Всё вышеперечисленное — структура, счёт, поимённое совпадение и грамматика строк. Диаграмма может быть структурно безупречной и не отрисоваться. Пока `mmdc` не поставлен, утверждение «39/51 диаграмм рендерятся» в любом отчёте — `стьяна` (объявление без действия), и я его не делаю.

---

## 7. Что требует решения, а не механической починки

Чинить не мой мандат в этом заходе, но два пункта **нельзя** чинить механически даже потом:

1. **Жанр леджера `ZooLink/CLAUDE.md` (упирается в F-3).** Он «журнал того, что сделал слайс» или «состояние на сейчас»? От ответа зависит починка: в первом случае к строке 0037 дописывается *«надстроено в c44874c: q вычисляется, `SavedSearch.Matched` эмитится»*, во втором — правится само утверждение. Сегодня файл смешивает жанры (исторические строки + сквозные «41 таблица»), поэтому обе починки защитимы, а угадывать нельзя. **→ владелец/architect.**
2. **Разрыв нумерации ADR 0023–0034 (F-7).** Зарезервированы намеренно (выравнивание на номера миграций) или это след потерянного пакета? Я вижу только отсутствие файлов и отсутствие объяснения. **→ architect.**

Кандидат в замок (тоже не мой вызов): непарность EN/RU ловится **детерминированно** — «в коммите есть `docs/x`, нет `docsRU/x`» это одна строка pre-commit-проверки, и она поймала бы F-1 и F-2 в момент написания, без участия модели. Ровно форма закона №10 (способность в инструменте, не в внимательности). **→ devops/architect**, если владелец сочтёт нужным.

---

## 8. Итог лейна

| Проверка | Результат |
|---|---|
| 1. Зеркальность файлов волны | 23/23 пары существуют · **2 расхождения содержания** (F-1 🔴, F-2 🔴) · дублей ключей 0 · битых ссылок 0 |
| 2. Леджер 0035–0040 | Счётчики 37→41 **верны** · идентификаторов 16/16 · констрейнтов 9/9 · **2 ложных утверждения в строке 0037** (F-3 🟡) |
| 3. Индексы ADR + §13 | **Чисто.** 6/6 Accepted в обеих ветках, Accepted не переписаны, §13 RESOLVED на месте · разрыв нумерации не объяснён (F-7 🟢) |
| 4. Перекрёстные ссылки | event-catalog ↔ spec 07 ↔ код **сходятся поимённо** · индекс спек 18/18 · **пробел покрытия глоссария** (F-5 🟡) |
| 5. Mermaid / ERD | ERD **41 = 41 поимённо**, 0 висячих рёбер, паритет 25/25 · **рендер НЕ проверен** (`mmdc` отсутствует) |

Волна оставила **два реальных расхождения зеркала** (оба — в `feat`-коммитах, оба в файлах, за которыми не следит ни один автоматический гейт) и **два случая просроченного самоописания** (леджер, шапка спеки 18). Каркас контракта — ADR, ERD, схема, событийный каталог, индексы — **цел и правдив**: счётчики сходятся с источником истины, имена констрейнтов совпадают, ни один Accepted ADR не переписан.

Найденное не чинил (граница задачи); рабочее дерево тронуто только этим файлом.
