# CLAUDE.md — ZooLink (project, implementation phase) · Claude Code adapter

> **Thin harness adapter.** Vendor-neutral knowledge is in `../agent-os/`. This file mounts
> the engineering core via `@import` and keeps the **living DB migration ledger** (the one
> piece of fast-changing project state). Full implementation protocol: `IMPLEMENTATION_PLAYBOOK.md`.

## Mounted core (auto-loaded)
@../agent-os/instructions/engineering-guide.md
@../agent-os/instructions/truth-hierarchy.md
@../agent-os/instructions/doc-code-protocol.md
@../agent-os/instructions/collaboration.md

## Team & orchestration
Roles are defined in `../agent-os/roster/*.md` (who-to-call: `../agent-os/roster/README.md`);
on Claude Code they are the subagents under `.claude/agents/` (generated — do not hand-edit).
The main session is the **orchestrator** (`../agent-os/roster/orchestrator.md`).

## DB state — living migration ledger (43 tables)
Schema is **not** generated by Prisma Migrate (ADR-0007). Migrations `0001`–`0041` idempotent:
- `0011` reference-seed · `0012` outbox relay delivery-state (attempts/last_error/next_attempt_at/dead_lettered_at)
- `0013` updated_at-triggers set by actual column presence (dropped wrong triggers on outbox_events/animal_ownership_history/messages, added missing on digital_assets)
- `0014` dropped redundant `idx_outbox_unprocessed` · `0015` `users.erased_at` (ФЗ-152 erase_user, Identity Slice 4)
- `0016` ADR-0011 A0a actor-snapshot: `actor_principal_type` on audit_log+moderation_decisions; `actor_role`/`supersedes_decision_id`/`is_human_override`+`chk_moddec_override` on moderation_decisions; role_in_org 4-canon hygiene (tables +0)
- `0017` ADR-0011 A0b `service_credentials` — agent-service-auth FORM (hashed-secret by AGENT principal, rotatable/revocable, gated, not seeded in MVP); tables +1 → 32
- `0018` A2 reference-data: species/breeds/cities `sort_order`/`created_by`/`updated_by` + localization flat→JSONB `name_localized {ru,en}` (drop name_ru/name_en, per-locale GIN) + `audit_log.entity_id_int`; tables +0 → 32
- `0019` A3 breeding dicts: `health_certifications` + `genetic_markers` INT lookups in A2 form (name_localized JSONB + market ADR-0002 + sort_order/is_active/created_by/updated_by, UNIQUE(market,code), seed×4+4); tables +2 → 34
- `0020` B2 refresh_tokens session-form (ip_address/user_agent/last_used_at/revoked_reason; no MFA placeholder — §5)
- `0021` B3 `listings.listing_type` +`leasing` (form now, behavior Phase 2); tables +0 → 34
- `0022` B10 `decision_templates` — INT lookup of candidate moderator phrasings (REJECT/CHANGES_REQUESTED) in A2/A3 form (body_localized JSONB {ru,en} + applies_to_decision + market + opt. related_reason_code FK→moderation_reasons + sort_order/is_active/provenance, UNIQUE(market,code), per-locale GIN, updated_at-trigger, seed×3); tables +1 → 35
- `0023` Animal Slice 2 ownership transfer (ADR-0013): `ownership_transfers` +CANCELLED status, +from/to_organization_id (exactly-one-of-user/org CHECK each side), +completed_at, +initiated/responded_by_principal_type (HUMAN|AGENT), +initiated_by_user_id/+responded_by_user_id (acting user; from_user_id is the from-party, NULL for org-from), +transfer_reason, +partial-unique `UNIQUE(animal_id) WHERE status='PENDING'` (INV-4); `animal_ownership_history` owner_id→nullable +organization_id +exactly-one-of CHECK (OQ-1=a); `trg_animals_immutable_and_owner` "block all"→GUC-controlled (`app.ownership_transfer='on'`, first `app.*` GUC) + duplicate def reconciled to one canonical body; seed `feature_toggles.ownership_transfer_verification`=off; tables +0 → 35
- `0024` Slice 4c (A) SLA-escalation: `listings.escalated_at` TIMESTAMPTZ NULL (idempotent-emission marker for `Moderation.Escalated`; set in the same tx as the outbox write, never mutates status — M-13/SLA-1/SLA-3) + partial index `idx_listings_escalation_scan` on `(moderation_enqueued_at) WHERE status='PENDING_MODERATION' AND escalated_at IS NULL`; advisory-lock key `MODERATION_ESCALATION_TICK=4202n`; tables +0 → 35
- `0025` fix-wave 1.1: `CREATE OR REPLACE` cascade_animal_deactivation/cascade_user_deactivation so the cascade to live listings also sets `is_active=false` (was status-only → drift from app paths listing.service.ts:293/393); is_active is DERIVED from status; tables +0 → 35
- `0026` fix-wave 1.1: reconcile ownership_transfers principal_type CHECK names — drop legacy auto-named `ownership_transfers_*_principal_type_check`, assert single canonical `chk_owntransfer_initiated_ptype`/`chk_owntransfer_responded_ptype` (matches database_schema.sql now + migration 0023). Kills schema↔migrations DDL drift → migration-drift CI gate promoted advisory→blocking; tables +0 → 35
- `0027` Impl-wave A / Q4 (GAP-BA-011): seed `feature_toggles.goods_marketplace` (FORM now, behaviour deferred; is_enabled=false/0%) — future goods marketplace (accessories/feed/vet-supplies) gate, same shape as payments/ownership_transfer_verification; data-only INSERT ON CONFLICT DO NOTHING, no code reads it yet; tables +0 → 35
- `0028` Impl-wave B / ADR-0019 (enforces ADR-0012) PII-at-rest crypto seam, T1 BLOCKER: `users.email_bidx` VARCHAR(60) (HMAC-SHA256 blind index over normalised email) + widen `users.email`+`users.contact_phone` VARCHAR→TEXT (hold AES-256-GCM `enc:v1:` ciphertext) + swap `idx_users_email`→`idx_users_email_bidx`. Code: `lib/crypto/CryptoService` (@Global CryptoModule; AES-256-GCM random-IV+tag, key from `PII_DATA_KEY`; blind-index HMAC from `PII_BLIND_INDEX_KEY`; both env min-32 no-default) — email write=encrypt+bidx, read=decrypt (legacy-plaintext passthrough), recovery+admin-search lookups repointed to bidx (admin email-search now EXACT via bidx, full_name stays substring), erase clears email_bidx. contact_phone=seam-ready/backfilled, no read-write path (contact-exchange=sub-wave C deferred). Companion idempotent backfill `prisma/backfill/0028_pii_backfill.ts` (74 dev emails encrypted+indexed). tables +0 → 35
- `0029` Wave C (ADR-0020) versioned consent-record model: new append-only `consents` (user_id subject/consent_type CHECK CONTACT_DISTRIBUTION live + MARKETING/ANALYTICS_PROFILING/NONESSENTIAL_COOKIES reserved/granted/policy_version/source/actor_id+actor_principal_type snapshot; idx_consents_current (user,type,created_at DESC) + agent partial idx; BEFORE UPDATE/DELETE immutability via reused trg_block_modify_append_only; current=latest row) + flip `users.contact_prefs` DEFAULT show_phone:true→false (all-OFF, ст.10.1; +2 code copies admin-user/retention) + dedup `contact_reveals` + UNIQUE(viewer_id,listing_id) `uq_contact_reveals_viewer_listing` (billing-unit fix). tables +1 → 36
- `0030` Wave C4 (ADR-0021) first real outbox consumer / IN_APP notification path: widen `notification_logs.type` CHECK `('EMAIL','SMS')`→`('EMAIL','SMS','IN_APP')` (auto-name replaced by stable `chk_notiflog_type` — drift-gate) + seed 5 EMAIL transfer-lifecycle templates ×(ru,en) `transfer_{initiated,accepted,declined,cancelled,expired}` (IN_APP renders from the EMAIL source row; channel≠source). Code: worker-only `NotificationConsumer` (first `OUTBOX_CONSUMERS`, registry-driven eventType→{template,recipients,context}, NOT '*'), idempotent INSERT…ON CONFLICT(idempotency_key=event.id‖recipient‖template)DO NOTHING, transactional-always (ignores notification_prefs, ФЗ-38), forward-only replay (relay `WHERE processed_at IS NULL`; guardrail = never prune outbox pre-analytics); transfer.service now emits `OwnershipTransfer.{Initiated,Accepted,Declined,Cancelled,Expired}` in-tx; +OrgMembershipService.orgAdminUserIds (org fan-out). tables +0 → 36
- `0031` Wave D / D1 views-capture (GAP-TRACE-006 · AUDIT3 data-analyst — the ONE irreversibly-lost signal, reserved FIRST): `listings.view_count BIGINT NOT NULL DEFAULT 0` + `chk_listings_view_count_nonneg` (view_count >= 0). Best-effort +1 on the public `GET /listings/{id}` detail read (Prisma atomic `increment`; SET view_count = view_count + 1), deduped per (viewer=userId or anon-IP, listing) in Redis (`listing-view:{id}:{viewerKey}` SET NX EX 1800 — 30-min window) so F5 doesn't inflate; only ACTIVE listings counted, seller self-views excluded; never throws (best-effort, doesn't gate the read). Code: listing.service `captureView` + `getById(id, actor, viewerIp)`, controller `@Ip()`; `getAnalytics.views` now real (was hard-0), `ListingView.viewCount` on single-get + list. NOT added: denormalized `contact_shown_count` (contact-show sourced from `contact_reveals`, no second counter to drift). tables +0 → 36
- `0033` Wave D / D3 derived-`market` cache (ADR-0018 §Amendment 2026-07-04, rule 6/7 — cycle-breaker for the list-path): `listings.market VARCHAR(9) NOT NULL` + named `chk_listings_market CHECK (market IN ('pet','livestock'))` + idempotent backfill from `animals ⋈ species` (nullable→backfill→SET NOT NULL) + `idx_listings_market_status(market,status)`. Caches the DERIVED market (species.market via animal), NOT the assigned `market_scope` tag (ADR-0015 rule 7). Write-only in D3: create-path writes it in-tx via `AnimalService.getOwnedAnimalForActor` (now returns `market`; nested species select keeps the D8 grep-gate green — no `FROM animals`/`JOIN species` in listing/moderation); admin species-market correction recomputes via `reference-data → ListingService.recomputeMarketForSpecies → AnimalService.animalIdsForSpecies` + listings-only `updateMany` (AdminModule now imports ListingModule, acyclic). No read path switches to the column (that is D8). tables +0 → 36
- `0032` Wave D / D2 OfferingRef form-now seam (ADR-0014 §Amendment 2026-07-04 rule 11): polymorphic offering reference on **both** `favorites` and `saved_searches` — `offering_type VARCHAR(30) NOT NULL DEFAULT 'ANIMAL_LISTING'` + extensible `chk_*_offering_type CHECK (offering_type IN ('ANIMAL_LISTING'))` (widened additively per subtype) + `offering_id UUID`. `favorites.offering_id` NOT NULL (truthful polymorphic pointer, backfilled from `listing_id`; today == listing_id; no FK — target is polymorphic, anti-god-table) + index `idx_favorites_offering(offering_type,offering_id)`; `saved_searches.offering_id` NULLABLE (a search is a query, not one offering — offering_type is the discriminator, offering_id stays NULL). Behaviour listing-only, unchanged. Contract: favorites-api `Favorite` + geo-search `SavedSearch`/`SavedSearchCreate` gain `offeringType`/`offeringId`; `favorites-api listingId` RETAINED as deprecated read-only alias == offeringId (non-breaking v1). Code: saved-search.service writes offering_type='ANIMAL_LISTING'/offering_id=null + maps to view. Precedes favorites controller (D11). tables +0 → 36
- `0034` Wave D / D6 multi-role junction (ADR-0022, Option 2): new `user_roles(id, user_id UUID FK→users ON DELETE CASCADE, role VARCHAR(20), actor_id UUID FK→users ON DELETE SET NULL, actor_principal_type HUMAN|AGENT, created_at)` + named constraints `chk_user_roles_role` (7-role canon, identical to users.role), `chk_user_roles_actor_ptype`, `uq_user_roles_user_role UNIQUE(user_id,role)` + `idx_user_roles_user`. **DORMANT**: `users.role` stays the primary/authoritative + sole authz source (rule 2) — no authz code reads the junction, a row grants NOTHING. Idempotent backfill = one row per user from current `users.role` (ON CONFLICT DO NOTHING). Code: additive **sync-on-write** on the existing admin role-change path (`AdminUserService.setRole` now wraps users.update + `user_roles.createMany({skipDuplicates})` in one `$transaction`, ADMIN actor snapshot); self-claim/revoke/authz-read deferred to first role-gated offering slice (rule 5). Authz-parity + write-only-junction pinned by `user-roles-junction.spec.ts`. tables +1 → 37
- `0035` Slice H2 / ADR-0035 listing ETag content-version (fixes P0-2 spurious 412 edit-lockout / view-flood griefing): `listings.content_updated_at TIMESTAMPTZ NOT NULL DEFAULT now()` — the dedicated CONTENT/state validator the listing ETag is derived from (NOT `updated_at`). Idempotent `IF NOT EXISTS` guard + one-time backfill from `updated_at` (fires only on first create, never clobbers app values on re-run); `DEFAULT now()` retained permanently for N-1 rolling-deploy write-compat. Same migration: `content_updated_at = now()` added to `cascade_animal_deactivation`/`cascade_user_deactivation` (CREATE OR REPLACE). Code: `etag()` → `weakEtag(listing:${id}, row.content_updated_at)`; every content/state writer (create via DEFAULT, `update` :343, `submit` :457, `withdraw` :496, `markSold` :684, moderation verdict flip) sets `content_updated_at` beside `updated_at`; the system writes `captureView`/`recomputeMarketForSpecies`/`escalated_at` deliberately do NOT (so a view never rotates the ETag). `getAnalytics` ETag unchanged (§5). Off-row counter (Option C) reserved as the Phase-2 scale evolution behind this seam. tables +0 → 37
- `0025` fix-wave 1.1 & `0035` amend the two cascade functions — the last `CREATE OR REPLACE` wins (0035 is the canonical body: sets status/is_active/updated_at/content_updated_at).
- `0036` Slice H2-B / AUDIT4 P1-2 consent monotonic tie-break (fixes fail-OPEN same-`created_at` consent resolution, ФЗ-152 ст.9 ч.2): `consents.seq BIGINT GENERATED ALWAYS AS IDENTITY` — a strictly-increasing DB-assigned insertion order + `idx_consents_seq_current(user_id, consent_type, seq DESC)`. `ConsentService.currentlyGranted` now `orderBy {seq: 'desc'}` (was `[created_at DESC, id DESC]`, where `id` is a random uuid_generate_v4() → two same-µs rows tie-broke on random UUID → a withdrawal could lose to the grant it supersedes and fail OPEN toward distribution). `seq` is a TOTAL order → deterministic causal resolution (row written last wins), fail-safe by causality (correct for withdraw-then-regrant too). Append-only `trg_consents_immutable` UNTOUCHED (additive DDL, not a row mutation). Idempotent DO-block `IF NOT EXISTS` guard; on a populated table ADD COLUMN back-fills existing rows in physical order + advances the sequence (re-run skipped → never re-assigns). N-1-SAFE: GENERATED ALWAYS = never app-written, so an N-1 pod INSERT without the column succeeds. tables +0 → 37
- `0037` Slice H4 / AUDIT4 saved-search → notify (first demand-side return loop): DATA-ONLY seed of the EMAIL `saved_search_matched` template ×(ru,en) (ON CONFLICT (name,type,language) DO NOTHING → idempotent run-twice; no DDL → trivially N-1-safe, out of scope for the backfill CI gate). Code (worker-only, no new endpoint/column): NEW `SavedSearchMatchConsumer` = the 2nd `OUTBOX_CONSUMERS` entry, consumes the existing in-tx `Listing.Activated` (moderation APPROVE→ACTIVE) and materializes one IN_APP `saved_search_matched` per matching (saved_search, listing) pair via NEW shared `NotificationWriter` (materialize extracted from NotificationConsumer, which now delegates). Dedup = deterministic per-pair `idempotency_key='saved_search_matched:<ssId>:<listingId>'` → exactly-once under `Listing.Activated` redelivery, NO marker column / NO `SavedSearch.Matched` event. Match = parameterized `$queryRaw` over `saved_searches` only (species_id/breed_id via Prisma nested select — gate-safe, reworded docstring avoids `FROM animals`/`JOIN species` grep-gate false-positive); subset = market/species_id/breed_id/listing_type equality + price bounds + geo Haversine, `q` NOT evaluated (documented). ADR-0002 enforced structurally: matches only market-ANCHORED searches (market pin ==listing OR species/breed pin), market-agnostic search NOT matched (no cross-market leak). Prefs: user-requested service notification over IN_APP → transactional-always, NOT gated by notification_prefs.promo (opt-out = delete the saved search). seller self-exclusion; not-ACTIVE-at-relay skip. tables +0 → 37
- `0038` Slice agent-auth (ADR-0036 credential issuance + ADR-0037 scoped-ability) — unstubs `service_credentials`: **§A** ALTER `service_credentials` +`issued_by` UUID FK users ON DELETE RESTRICT (accountable human issuer-of-record, ADR-0006 #5) +`last_used_at` TIMESTAMPTZ (best-effort stale detect) +`expires_at` TIMESTAMPTZ (optional auto-expiry, form now/90d-default at activation) +`capability_profile_id` INT FK agent_capability_profiles ON DELETE RESTRICT (all additive/nullable → N-1 safe); **§B** NEW `agent_capability_profiles` (SERIAL id, `code` VARCHAR(50) UNIQUE=`uq_agent_capability_profiles_code`, `scope` JSONB, is_active, created_by/updated_by, updated_at-trigger) = named least-privilege {action,subject} sets, scope NEVER manage/all (service validator); **§C** seed `feature_toggles.agent_service_auth`=OFF (master gate, ADR-0036 §6, payments-shape); **§D** seed reference profile `moderation-agent` scope=[read ModerationQueue,create ModerationDecision,read Listing,read ContentReport] (ADR-0037 §3). Code: token-exchange `POST /v1/auth/agent/token` (@Public, master-gate off→403, parse credId→O(1) load→is_active∧¬revoked∧¬expired→constant-time HMAC→single 401 any-fail no-oracle, per-credId+IP Redis rate-limit, short-lived AGENT JWT via signAccess + resolved scope claim, NO refresh); admin issuance `POST|DELETE|POST .../rotate /v1/admin/agents/{agentUserId}/credentials` (ADMIN-only, plaintext `zlk_agent_<credId>_<secret>` once, HMAC via CryptoService.agentCredentialHash, audit HUMAN actor secret-never-logged); AbilityFactory AGENT branch = deny-by-default effective=matrix(role)∩scope, manage:all NEVER emitted for AGENT, HUMAN path byte-identical; deactivate-agent (AdminUserService) cascade-revokes live creds in same tx. MVP: master gate off, no agent provisioned, HUMAN behaviour byte-identical. tables +1 → 38
- `0039` Slice reputation FORM-#1 (ADR-0038 confirmed-sale record-of-truth): **§A** NEW `confirmed_sales` — FIRST-CLASS append-only (reused `trg_block_modify_append_only` → `trg_confirmed_sales_immutable`, NOT a 2nd immutability path) proof-of-transaction root (reviews FK target, ADR-0039). Cols: polymorphic `offering_type` DEFAULT 'ANIMAL_LISTING' (widen-additively CHECK, mig 0032 dialect) + `offering_id` (listing pointer, NULL for pure transfer) + concrete `listing_id`/`animal_id`; derived `market` NOT NULL CHECK pet|livestock (ADR-0018/0033, cached at capture); parties seller/buyer (user OR org, FK ON DELETE SET NULL = ФЗ-152 pseudonymise); `anchor_type` CHECK TRANSFER|LISTING_MARK_SOLD; `ownership_transfer_id` FK + `UNIQUE`=one-sale-per-transfer (mirror INV-4); `status` CHECK PENDING_CONFIRMATION|CONFIRMED|DISPUTED|EXPIRED|CANCELLED (full vocab reserved; FORM slice writes ONLY CONFIRMED); `nominated_buyer_user_id` RESERVED (markSold path); `amount_minor` BIGINT NULL RESERVED off-record (owner 2026-07-09 — NO path writes it); actor-snapshot `actor_id`+`actor_principal_type` HUMAN|AGENT (named CHECK); idx subject/parties/animal + partial confirm_scan. **§B** seed `feature_toggles.sale_buyer_confirmation`=OFF (markSold counter-confirm gate; `reputation_reviews`=NOT this slice, ADR-0039). **Passive capture (heart)**: transfer.service.accept() tx (PENDING→COMPLETED — NB there is NO `OwnershipTransfer.Completed` event; completion transition emits `OwnershipTransfer.Accepted`) now ALSO INSERTs an auto-CONFIRMED confirmed_sales row + emits `ConfirmedSale.Confirmed` outbox event, IN THE SAME TX (atomic — INSERT failure rolls back the whole accept → transfer never completes without the truth row). **Created/Confirmed ambiguity RESOLVED**: transfer path emits ONLY `ConfirmedSale.Confirmed` (row born CONFIRMED, no PENDING phase per spec-18 §4 `[*]→CONFIRMED`); `ConfirmedSale.Created` reserved for the deferred markSold PENDING path. Seller=FROM-party, buyer=TO-party, actor=responding(accepting) actor. Dedup=accept status-guard single-winner + UNIQUE(transfer_id) backstop → exactly-once under redelivery/parallel-accept. derived market REUSES the already-computed intra-aggregate `market` in accept() (animal module, ADR-0018; grep-gate green — no new join). GOTCHA append-only cleanup in e2e = `ALTER TABLE confirmed_sales DISABLE/ENABLE TRIGGER` (consents idiom); confirmed_sales.market NOT NULL narrowed via `market==='livestock'?…:'pet'` (species.market NOT NULL → non-null in practice). DORMANT: no consumer, no endpoint reads it, HUMAN behaviour byte-identical. FLAGGED (behaviour slice/architect): append-only vs markSold PENDING→CONFIRMED transition mechanics (superseding-row vs relax) deferred. tables +1 → 39
- `0040` Slice reputation FORM-#2 (ADR-0039 storage model) — DORMANT reviews+aggregates storage the review/reputation loop hangs off; no code reads/writes them, HUMAN behaviour byte-identical. **§A** NEW `reviews` — append-only (reused `trg_block_modify_append_only` → `trg_reviews_immutable`) proof-of-transaction review: FK `confirmed_sale_id`→confirmed_sales ON DELETE CASCADE (no sale⇒no review); `direction` CHECK BUYER_ON_SELLER|SELLER_ON_BUYER; `rating` SMALLINT CHECK 1..5; `body` TEXT NULL; **`seq` BIGINT GENERATED ALWAYS AS IDENTITY** = monotonic current/supersede order (mig 0036 lesson, NOT created_at); `is_visible` DEFAULT FALSE (double-blind fork 2); `moderation_status` CHECK PENDING|APPROVED|REJECTED|CHANGES_REQUESTED (ADR-0040 §2); 3 nullable DORMANT facet cols (fork 5, no CHECK); `market` copy CHECK pet|livestock; party FKs `reviewer_user_id`/`subject_user_id` ON DELETE SET NULL (ФЗ-152 pseudonymise, fork 8 — NULLABLE, overrides sketch's self-contradictory NOT NULL+SET NULL); actor-snapshot `actor_id`+`actor_principal_type` HUMAN|AGENT (authoring OFF ADR-0040 §3); named CHECKs. **one-CURRENT-per-(sale,direction) = partial-unique `uq_reviews_current_per_direction WHERE superseded_by_id IS NULL`** (transfer INV-4 shape — plain 3-col UNIQUE ineffective b/c NULLs distinct). **FLAGGED architect/behaviour-slice**: forward `superseded_by_id` conflicts with append-only (marking predecessor needs a blocked UPDATE) → behaviour slice picks (i) resolve-by-seq-DESC (drop partial-unique) OR (ii) backward `supersedes_review_id`+UNIQUE (à la moderation_decisions); FORM ships ADR-named col + correct-and-INERT head partial-unique. **§B** NEW `reputation_aggregates` — DERIVED cache, **PK (subject_user_id, market)** = ADR-0002 boundary; **`rating_avg` NUMERIC(3,2) GENERATED ALWAYS AS ROUND(rating_sum::numeric/NULLIF(review_count,0),2) STORED** = structurally recompute-only/unpurchasable (fork 7/TP-8, stronger than a comment; NULL at count=0); dist_1..5 histogram; NOT append-only (mutable recompute cache — proven by an UPDATE test); nonneg+market CHECKs; ADR-0040 §1 weighting hook additive (not a col). **§C** `consents.consent_type` CHECK widened +`REVIEW_PUBLICATION` (ADR-0039 §4 — reuse consents model, no 2nd store; auto-name 0029→named `chk_consents_consent_type` drift-gate; N-1 safe widen). **§D** seed `feature_toggles.reputation_reviews`=OFF (master gate incl §4 erasure, also legal-gated). GOTCHAs: reviews append-only cleanup=DISABLE/ENABLE TRIGGER (consents idiom); bogus `direction` must fit VARCHAR(16) else 22001 length err masks the CHECK (use 'MUTUAL'); Prisma introspects GENERATED as `@default(dbgenerated())`. 701u UNCHANGED (no code)/e2e 401+9todo (+16: reputation-storage m2/m3 all invariants incl rating 0&6, GENERATED seq/avg reject, mutable-cache positive), mig×2 idempotent, market-gate/typecheck/lint green. tables +2 → 41
- `0041` AUDIT5 reputation reform COMMIT-1 (ADR-0038 §4 Amendment + ADR-0039 §3 Amendment; держатель gate-pass 2026-08-04) — resolves the FORM-slice self-contradiction "state MACHINE on APPEND-ONLY tables" by splitting **immutable FACT from mutable STATE** (project pattern now covers sales AND reviews). DORMANT/FORM (reputation_reviews/sale_buyer_confirmation stay OFF, HUMAN behaviour byte-identical). **§A** `confirmed_sales` → CONFIRMED-ONLY FACT: DROP negotiation cols (nominated_buyer_user_id/seller_confirmed_at/buyer_confirmed_at/expires_at) + status DROP DEFAULT + narrow `chk_confirmed_sales_status` to `CHECK(status='CONFIRMED')` (Q2 gravestone, COMMENT on constraint) + DROP idx_confirmed_sales_confirm_scan; **uq_confirmed_sales_transfer stays FULL** (Q1 — confirmed transfer-sale permanent, cancel/dispute-after = new record ADR-0040); KEEP confirmed_at. **§B** NEW mutable `sale_confirmations` (markSold PENDING→CONFIRMED/EXPIRED/CANCELLED/DISPUTED; updated_at auto-trigger; `chk_sale_conf_confirmed_link` biconditional mirror of chk_moddec_override = CONFIRMED⇔confirmed_sale_id set; `uq_sale_conf_live_per_listing` partial one-live-per-listing; TRANSFER path never writes here). **§C** `reviews`: FORWARD superseded_by_id → BACKWARD `supersedes_review_id` (ON DELETE RESTRICT, moderation_decisions model, append-only-safe write-about-self); DROP uq_reviews_current_per_direction; ADD `uq_reviews_supersedes` partial (≤1 successor=LINEAR chain, Q5 DELIBERATELY stricter than moderation_decisions — COMMENT warns don't-unify) + `uq_reviews_root_per_direction` partial (one root⇒one head⇒one-current stays DB-invariant, Q6); rebuild idx_reviews_subject_market w/o departed predicate cols; VIEW `reviews_current` (NOT EXISTS-successor) = single named current resolver; seq/idx_reviews_current_seq stay. **§D** NEW mutable `review_states` (review_id PK FK reviews CASCADE; moderation_status+is_visible moved off append-only reviews; updated_at auto-trigger; back-fill guarded DO-block). transfer.service stops writing dropped cols (confirmed_at covers). GOTCHAs: updated_at triggers auto-attached by the derived DO-block (name `update_<tbl>_updated_at`) — migration adds them explicitly since migration path has no DO-block; e2e reviews cleanup deletes CHILDREN before ROOTS (supersedes RESTRICT); mkSale writes CONFIRMED now; column-order divergence (ALTER ADD appends supersedes_review_id) is cosmetic, drift-gate GREEN (new-canon+migration=no-op verified). MUTATION ACCEPTANCE proven RED-under-mutation on throwaway-from-canon: M-1 (rm uq_reviews_root_per_direction→2 roots pass), M-3 (rm uq_reviews_supersedes→history fork passes), M-4 (rm trg_confirmed_sales_immutable→UPDATE passes), each isolated + baseline all-green; migration idempotent ×2 on live PG; 712u/e2e reputation-storage 24+confirmed-sales 19+transfer 30 green; typecheck/lint/build/grep-gates green. tables +2 → 43. NOT committed

~two dozen invariants enforced. Reference + reasons/templates seeded by `npm run seed` (idempotent).
The DB-change workflow, API conventions, stack, commands, and audits are in the imported
engineering-guide + doc-code-protocol.
