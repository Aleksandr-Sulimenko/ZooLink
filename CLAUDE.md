# CLAUDE.md — ZooLink (project, implementation phase) · Claude Code adapter

> **Thin harness adapter.** Vendor-neutral knowledge is in `../agent-os/`. This file mounts
> the engineering core via `@import` and keeps the **living DB migration ledger** (the one
> piece of fast-changing project state). Full implementation protocol: `IMPLEMENTATION_PLAYBOOK.md`.

## Mounted core (auto-loaded)
@../agent-os/instructions/engineering-guide.md
@../agent-os/instructions/truth-hierarchy.md
@../agent-os/instructions/doc-code-protocol.md
@../agent-os/instructions/collaboration.md

## Team & orchestration
Roles are defined in `../agent-os/roster/*.md` (who-to-call: `../agent-os/roster/README.md`);
on Claude Code they are the subagents under `.claude/agents/` (generated — do not hand-edit).
The main session is the **orchestrator** (`../agent-os/roster/orchestrator.md`).

## DB state — living migration ledger (35 tables)
Schema is **not** generated by Prisma Migrate (ADR-0007). Migrations `0001`–`0022` idempotent:
- `0011` reference-seed · `0012` outbox relay delivery-state (attempts/last_error/next_attempt_at/dead_lettered_at)
- `0013` updated_at-triggers set by actual column presence (dropped wrong triggers on outbox_events/animal_ownership_history/messages, added missing on digital_assets)
- `0014` dropped redundant `idx_outbox_unprocessed` · `0015` `users.erased_at` (ФЗ-152 erase_user, Identity Slice 4)
- `0016` ADR-0011 A0a actor-snapshot: `actor_principal_type` on audit_log+moderation_decisions; `actor_role`/`supersedes_decision_id`/`is_human_override`+`chk_moddec_override` on moderation_decisions; role_in_org 4-canon hygiene (tables +0)
- `0017` ADR-0011 A0b `service_credentials` — agent-service-auth FORM (hashed-secret by AGENT principal, rotatable/revocable, gated, not seeded in MVP); tables +1 → 32
- `0018` A2 reference-data: species/breeds/cities `sort_order`/`created_by`/`updated_by` + localization flat→JSONB `name_localized {ru,en}` (drop name_ru/name_en, per-locale GIN) + `audit_log.entity_id_int`; tables +0 → 32
- `0019` A3 breeding dicts: `health_certifications` + `genetic_markers` INT lookups in A2 form (name_localized JSONB + market ADR-0002 + sort_order/is_active/created_by/updated_by, UNIQUE(market,code), seed×4+4); tables +2 → 34
- `0020` B2 refresh_tokens session-form (ip_address/user_agent/last_used_at/revoked_reason; no MFA placeholder — §5)
- `0021` B3 `listings.listing_type` +`leasing` (form now, behavior Phase 2); tables +0 → 34
- `0022` B10 `decision_templates` — INT lookup of candidate moderator phrasings (REJECT/CHANGES_REQUESTED) in A2/A3 form (body_localized JSONB {ru,en} + applies_to_decision + market + opt. related_reason_code FK→moderation_reasons + sort_order/is_active/provenance, UNIQUE(market,code), per-locale GIN, updated_at-trigger, seed×3); tables +1 → 35
- `0023` Animal Slice 2 ownership transfer (ADR-0013): `ownership_transfers` +CANCELLED status, +from/to_organization_id (exactly-one-of-user/org CHECK each side), +completed_at, +initiated/responded_by_principal_type (HUMAN|AGENT), +initiated_by_user_id/+responded_by_user_id (acting user; from_user_id is the from-party, NULL for org-from), +transfer_reason, +partial-unique `UNIQUE(animal_id) WHERE status='PENDING'` (INV-4); `animal_ownership_history` owner_id→nullable +organization_id +exactly-one-of CHECK (OQ-1=a); `trg_animals_immutable_and_owner` "block all"→GUC-controlled (`app.ownership_transfer='on'`, first `app.*` GUC) + duplicate def reconciled to one canonical body; seed `feature_toggles.ownership_transfer_verification`=off; tables +0 → 35
- `0024` Slice 4c (A) SLA-escalation: `listings.escalated_at` TIMESTAMPTZ NULL (idempotent-emission marker for `Moderation.Escalated`; set in the same tx as the outbox write, never mutates status — M-13/SLA-1/SLA-3) + partial index `idx_listings_escalation_scan` on `(moderation_enqueued_at) WHERE status='PENDING_MODERATION' AND escalated_at IS NULL`; advisory-lock key `MODERATION_ESCALATION_TICK=4202n`; tables +0 → 35

~two dozen invariants enforced. Reference + reasons/templates seeded by `npm run seed` (idempotent).
The DB-change workflow, API conventions, stack, commands, and audits are in the imported
engineering-guide + doc-code-protocol.
