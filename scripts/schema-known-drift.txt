# ИЗВЕСТНОЕ И ПРИНЯТОЕ РАСХОЖДЕНИЕ ЖИВОЙ БД С КАНОНОМ — по одной строке дампа на строку файла.
# Заведено 20.08.2026 при закрытии долга «схема живой БД с 07.08 не сверялась с каноном».
# ЗАЧЕМ ОТДЕЛЬНЫЙ ФАЙЛ, А НЕ ПОРОГ В КОДЕ: без него прибор краснеет ВСЕГДА, и новое расхождение
# неотличимо от старого — то есть красное перестаёт что-либо значить (замерено: подсунутая лишняя
# колонка дала тот же rc=1, что и обычный прогон; различала только НАДПИСЬ).
# ЧТО ЗДЕСЬ ЛЕЖИТ СЕЙЧАС: порядок колонок таблицы reviews и её вью — supersedes_review_id в живой БД
# стоит В КОНЦЕ (её добавила поздняя миграция ALTER TABLE), а канон объявляет её после body.
# Структурно схемы совпадают: 44 таблиц/вью · 202 индекса · 242 ограничения · 519 колонок с обеих
# сторон. Снимет расхождение пересборка стенда из канона — это РЕШЕНИЕ, а не умолчание.
created_at
created_at,
supersedes_review_id
supersedes_review_id,
USING ERRCODE = 'ZL042';
USING ERRCODE = 'ZL042'; -- счётный маркер (Г-М2): provision.ts считает по КОДУ, не по тексту
