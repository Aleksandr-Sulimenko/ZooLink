openapi: 3.0.3
info:
  title: ZooLink Ownership-Transfer API
  version: 1.0.0
  description: >
    Animal ownership-transfer API for ZooLink (Animal Slice 2). Implements the **simplified
    direct transfer** ratified in ADR-0013: current owner initiates → recipient (user OR
    organization) accepts/declines; initiator may cancel a still-PENDING transfer; an
    unaccepted transfer expires after 72h (lazy-on-read, OQ-2). On accept the animal is
    atomically re-attributed and `animal_ownership_history` is appended in one transaction
    (under the `app.ownership_transfer` GUC, ADR-0013 §2).

    Conforms to API_CONVENTIONS.md: camelCase bodies (§0), `/api/v1` base (§1), bearer auth (§2),
    `x-required-roles` (§3, rbac-matrix.md), RFC7807 Problem errors (§4), `{items, meta: PageMeta}`
    pagination (§5), `Idempotency-Key` on unsafe POST (§11), `ETag`/`If-Match` optimistic
    concurrency on the accept/decline/cancel state-transition POSTs (§10), and the shared
    `Actor {actorId, principalType}` form for actor-bearing fields (§15, ADR-0011 §6 — a transfer
    may be initiated/answered by a HUMAN or AGENT principal).

    The heavy verification phase (IN_PROGRESS / payment / vet / legal-CITES / two-sided ack) is
    DEFERRED behind `feature_toggles.ownership_transfer_verification` (default off, ADR-0013 §1/§3)
    and is NOT part of this MVP contract. `FAILED` is reserved for that Phase-2 flow; MVP terminals
    are COMPLETED and CANCELLED only.
servers:
  - url: /api/v1
    description: ZooLink API v1
security:
  - bearerAuth: []
paths:
  /animals/{id}/transfers:
    post:
      summary: Initiate an ownership transfer of an animal
      operationId: initiateTransfer
      description: >
        Creates a PENDING transfer of animal `{id}` to a recipient selected by **exactly one of**
        three mutually-exclusive selectors (C5): `claimCode` (RECOMMENDED — a recipient-minted,
        single-use, opt-in transfer claim code, see `POST /transfers/claim-codes`), `toUserId`, or
        `toOrganizationId`. **No open user-search endpoint exists** (deliberate, to avoid a PII
        enumeration oracle — C5/security): a normal user names a recipient either by entering a
        code that recipient shared out-of-band, or by a `userId`/`organizationId` they already
        legitimately hold from an in-system relationship (e.g. a `contact_reveals` row). When
        `claimCode` is supplied it is validated and **consumed atomically** (single-use); the
        server resolves it to the recipient user (or the org the recipient nominated) — the
        initiator never learns recipient PII from this step. An invalid / expired / already-used /
        malformed code returns a **uniform** 422 `TRANSFER_CLAIM_CODE_INVALID` (no distinction
        between the failure modes — no existence oracle). For the raw `toUserId`/`toOrganizationId`
        selectors, a recipient that does not exist returns a **generic** 404 `RECIPIENT_NOT_FOUND`
        that does NOT distinguish user-vs-org or exists-vs-absent (closes the AUDIT3 MINOR
        enumeration oracle); initiate is additionally rate-limited (429 `RATE_LIMITED` +
        `Retry-After`). Object-level rule: the caller MUST be the animal's **current owner** (its
        present `ownerId`, or an authorized org-admin of its present `organizationId`). The
        resolved recipient MUST NOT be the current owner / the caller (no self-transfer → 422
        `SELF_TRANSFER`, enforced after code resolution too). At most one active PENDING transfer
        may exist per animal at a time; a second initiate while one is PENDING → 409
        `TRANSFER_ALREADY_PENDING` (DB-enforced by `UNIQUE (animal_id) WHERE status='PENDING'`).
        `expiresAt` is set to `now() + 72h` (OQ-2). The acting principal is snapshotted as
        `initiatedBy` `{actorId, principalType}` (ADR-0011 §6).
      x-required-roles: [USER, BREEDER, FARMER, VETERINARIAN, GROOMER, MODERATOR, ADMIN]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: string, format: uuid }
          description: Animal ID whose ownership is being transferred
        - in: header
          name: Idempotency-Key
          required: true
          schema: { type: string, format: uuid }
          description: Client-generated key for safe retry (API_CONVENTIONS §11, 24h window)
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/TransferInitiate' }
      responses:
        '201':
          description: Transfer created (PENDING)
          headers:
            ETag:
              schema: { type: string }
              description: Weak validator of the new transfer (send as If-Match on accept/decline/cancel, §10)
            Location:
              schema: { type: string }
              description: URI of the created transfer (`/api/v1/transfers/{transferId}`)
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Transfer' }
        '400': { $ref: '#/components/responses/Problem' }   # malformed body
        '401': { $ref: '#/components/responses/Problem' }
        '403': { $ref: '#/components/responses/Problem' }   # caller is not the current owner
        '404': { $ref: '#/components/responses/Problem' }   # animal not found / RECIPIENT_NOT_FOUND (generic, no user-vs-org leak)
        '409': { $ref: '#/components/responses/Problem' }   # TRANSFER_ALREADY_PENDING
        '422': { $ref: '#/components/responses/Problem' }   # SELF_TRANSFER / RECIPIENT_AMBIGUOUS / RECIPIENT_REQUIRED / TRANSFER_CLAIM_CODE_INVALID / Idempotency-Key reuse w/ different body
        '429': { $ref: '#/components/responses/Problem' }   # RATE_LIMITED (initiate throttle) — Retry-After
        '500': { $ref: '#/components/responses/Problem' }
  /transfers/claim-codes:
    post:
      summary: Mint a single-use transfer claim code (recipient opt-in)
      operationId: mintTransferClaimCode
      description: >
        The **recipient** mints a short-lived, single-use **transfer claim code** and shares it
        out-of-band (read aloud / QR / message) with the current owner, who enters it into
        `initiateTransfer` (`claimCode`). This is ZooLink's MVP-safe counterparty-resolution
        mechanism (C5): it needs **no user-search**, so it creates **no PII enumeration oracle** —
        the code is high-entropy and per-issuance (never per-identity), so a guessed code reveals
        nothing about who exists. Minting the code IS the recipient's opt-in to receive an incoming
        transfer; recipient PII is never disclosed to the initiator (the existing accept-flow stays
        the second consent gate). The code resolves to the authenticated caller as recipient user;
        if `forOrganizationId` is supplied the caller MUST be an authorized org-admin and the code
        instead nominates that org as the recipient. Codes are stored server-side (Redis, reusing
        the namespace-parameterised OTP lifecycle, ns=`transfer:claim`), TTL `expiresAt =
        now() + 15m`, single-use (consumed atomically on a successful initiate). Rate-limited per
        principal (429 `RATE_LIMITED` + `Retry-After`). The plaintext code is returned **once** in
        this response and is never retrievable again.
      x-required-roles: [USER, BREEDER, FARMER, VETERINARIAN, GROOMER, MODERATOR, ADMIN]
      parameters:
        - in: header
          name: Idempotency-Key
          required: true
          schema: { type: string, format: uuid }
          description: Client-generated key for safe retry (API_CONVENTIONS §11); a replay returns the same code+expiresAt within the window
      requestBody:
        required: false
        content:
          application/json:
            schema: { $ref: '#/components/schemas/ClaimCodeRequest' }
      responses:
        '201':
          description: Claim code minted (plaintext returned once)
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ClaimCode' }
        '400': { $ref: '#/components/responses/Problem' }
        '401': { $ref: '#/components/responses/Problem' }
        '403': { $ref: '#/components/responses/Problem' }   # forOrganizationId set but caller is not an org-admin of it
        '429': { $ref: '#/components/responses/Problem' }   # RATE_LIMITED — Retry-After
        '500': { $ref: '#/components/responses/Problem' }
  /transfers/{transferId}/accept:
    post:
      summary: Accept an incoming ownership transfer (recipient)
      operationId: acceptTransfer
      description: >
        Recipient accepts a PENDING transfer. Object-level rule: the caller MUST be the named
        recipient (`toUserId`, or an authorized org-admin of `toOrganizationId`). On accept, in
        ONE transaction (ADR-0013 §3 invariant 4): the animal's `ownerId`/`organizationId` is
        atomically re-attributed (under the `app.ownership_transfer` GUC), the transfer moves
        PENDING → COMPLETED with `completedAt` set, `animal_ownership_history` is appended
        (close the prior interval `endDate`, open the new interval `startDate`), and
        `animals.ownedSince` is updated. Idempotent under `Idempotency-Key`; a replay returns the
        stored COMPLETED transfer. `If-Match` (the transfer ETag) guards against acting on a
        stale view (e.g. a concurrent cancel). If the transfer is past `expiresAt` it is treated
        as expired on read (OQ-2 lazy): the accept is rejected 409 `TRANSFER_EXPIRED` and the
        transfer is transitioned to CANCELLED(`expired`).
      x-required-roles: [USER, BREEDER, FARMER, VETERINARIAN, GROOMER, MODERATOR, ADMIN]
      parameters:
        - in: path
          name: transferId
          required: true
          schema: { type: string, format: uuid }
        - in: header
          name: Idempotency-Key
          required: true
          schema: { type: string, format: uuid }
          description: Client-generated key for safe retry (§11); makes accept replay-safe
        - in: header
          name: If-Match
          required: true
          schema: { type: string }
          description: ETag from a prior GET /transfers/{transferId} (§10)
      responses:
        '200':
          description: Transfer accepted and COMPLETED (animal re-attributed, history appended)
          headers:
            ETag:
              schema: { type: string }
              description: New weak validator after completion
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Transfer' }
        '400': { $ref: '#/components/responses/Problem' }
        '401': { $ref: '#/components/responses/Problem' }
        '403': { $ref: '#/components/responses/Problem' }   # caller is not the named recipient
        '404': { $ref: '#/components/responses/Problem' }
        '409': { $ref: '#/components/responses/Problem' }   # TRANSFER_NOT_PENDING / TRANSFER_EXPIRED
        '412': { $ref: '#/components/responses/Problem' }   # STALE_RESOURCE (If-Match mismatch)
        '422': { $ref: '#/components/responses/Problem' }   # Idempotency-Key reuse w/ different body
        '428': { $ref: '#/components/responses/Problem' }   # If-Match missing
        '500': { $ref: '#/components/responses/Problem' }
  /transfers/{transferId}/decline:
    post:
      summary: Decline an incoming ownership transfer (recipient)
      operationId: declineTransfer
      description: >
        Recipient declines a PENDING transfer → CANCELLED with `terminalReason='declined'`.
        Object-level rule: the caller MUST be the named recipient (`toUserId`, or an authorized
        org-admin of `toOrganizationId`). The animal is NOT re-attributed. The acting principal
        is snapshotted as `respondedBy`. `If-Match` guards against acting on a stale view. A
        decline on an already-expired transfer is accepted as a no-op equivalent to the lazy
        expiry (the result is CANCELLED either way).
      x-required-roles: [USER, BREEDER, FARMER, VETERINARIAN, GROOMER, MODERATOR, ADMIN]
      parameters:
        - in: path
          name: transferId
          required: true
          schema: { type: string, format: uuid }
        - in: header
          name: If-Match
          required: true
          schema: { type: string }
          description: ETag from a prior GET /transfers/{transferId} (§10)
      responses:
        '200':
          description: Transfer declined and CANCELLED
          headers:
            ETag:
              schema: { type: string }
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Transfer' }
        '400': { $ref: '#/components/responses/Problem' }
        '401': { $ref: '#/components/responses/Problem' }
        '403': { $ref: '#/components/responses/Problem' }   # caller is not the named recipient
        '404': { $ref: '#/components/responses/Problem' }
        '409': { $ref: '#/components/responses/Problem' }   # TRANSFER_NOT_PENDING (already terminal)
        '412': { $ref: '#/components/responses/Problem' }
        '428': { $ref: '#/components/responses/Problem' }
        '500': { $ref: '#/components/responses/Problem' }
  /transfers/{transferId}/cancel:
    post:
      summary: Cancel a still-PENDING ownership transfer (initiator)
      operationId: cancelTransfer
      description: >
        Initiator cancels a PENDING transfer → CANCELLED with `terminalReason='cancelled_by_initiator'`.
        Object-level rule: the caller MUST be the **initiator** (the principal that created the
        transfer; an authorized org-admin of the from-org if the initiator side was an org). The
        animal is NOT re-attributed. `If-Match` guards against acting on a stale view (e.g. a
        concurrent accept that already completed it).
      x-required-roles: [USER, BREEDER, FARMER, VETERINARIAN, GROOMER, MODERATOR, ADMIN]
      parameters:
        - in: path
          name: transferId
          required: true
          schema: { type: string, format: uuid }
        - in: header
          name: If-Match
          required: true
          schema: { type: string }
          description: ETag from a prior GET /transfers/{transferId} (§10)
      responses:
        '200':
          description: Transfer cancelled
          headers:
            ETag:
              schema: { type: string }
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Transfer' }
        '400': { $ref: '#/components/responses/Problem' }
        '401': { $ref: '#/components/responses/Problem' }
        '403': { $ref: '#/components/responses/Problem' }   # caller is not the initiator
        '404': { $ref: '#/components/responses/Problem' }
        '409': { $ref: '#/components/responses/Problem' }   # TRANSFER_NOT_PENDING (already terminal)
        '412': { $ref: '#/components/responses/Problem' }
        '428': { $ref: '#/components/responses/Problem' }
        '500': { $ref: '#/components/responses/Problem' }
  /transfers/{transferId}:
    get:
      summary: Get a single ownership transfer
      operationId: getTransferById
      description: >
        Read one transfer. Object-level rule: visible to the **initiator**, the **recipient**
        (or an org-admin of either party org), and to **MODERATOR**/**ADMIN** (R). A caller who
        is none of these → 403. If the transfer is PENDING but past `expiresAt`, the read
        reflects it as effectively expired and transitions it to CANCELLED(`expired`) lazily
        (OQ-2) — the returned `status` is CANCELLED with `terminalReason='expired'`.
      x-required-roles: [USER, BREEDER, FARMER, VETERINARIAN, GROOMER, MODERATOR, ADMIN]
      parameters:
        - in: path
          name: transferId
          required: true
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Transfer details
          headers:
            ETag:
              schema: { type: string }
              description: Weak validator to send back as If-Match on accept/decline/cancel (§10)
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Transfer' }
        '401': { $ref: '#/components/responses/Problem' }
        '403': { $ref: '#/components/responses/Problem' }
        '404': { $ref: '#/components/responses/Problem' }
        '500': { $ref: '#/components/responses/Problem' }
  /transfers:
    get:
      summary: List the authenticated principal's transfers
      operationId: listMyTransfers
      description: >
        Lists transfers in which the authenticated principal is a party. `role=initiated`
        returns transfers the principal initiated; `role=incoming` returns transfers addressed
        to the principal (or an org the principal admins). Filter by `status`. PENDING items
        that are past `expiresAt` are surfaced as CANCELLED(`expired`) per the lazy-expiry rule
        (OQ-2). MODERATOR/ADMIN use the moderation/admin surfaces for cross-party listing — this
        endpoint is always principal-scoped.
      x-required-roles: [USER, BREEDER, FARMER, VETERINARIAN, GROOMER, MODERATOR, ADMIN]
      parameters:
        - in: query
          name: role
          required: true
          schema: { type: string, enum: [initiated, incoming] }
          description: Which side of the transfer to list for the authenticated principal
        - in: query
          name: status
          schema: { type: string, enum: [PENDING, COMPLETED, CANCELLED] }
          description: Optional filter by transfer status
        - in: query
          name: animalId
          schema: { type: string, format: uuid }
          description: Optional filter by animal
        - in: query
          name: sort
          schema: { type: string, example: 'created_at:desc' }
          description: Sort `<field>:<asc|desc>` (snake_case fields, §12); default `created_at:desc`
        - in: query
          name: page
          schema: { type: integer, minimum: 1, default: 1 }
        - in: query
          name: limit
          schema: { type: integer, minimum: 1, maximum: 100, default: 20 }
      responses:
        '200':
          description: Paginated list of the principal's transfers
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items: { $ref: '#/components/schemas/Transfer' }
                  meta: { $ref: '#/components/schemas/PageMeta' }
        '400': { $ref: '#/components/responses/Problem' }   # missing/invalid role
        '401': { $ref: '#/components/responses/Problem' }
        '403': { $ref: '#/components/responses/Problem' }
        '500': { $ref: '#/components/responses/Problem' }
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
  schemas:
    PageMeta:
      type: object
      properties:
        page: { type: integer }
        limit: { type: integer }
        total: { type: integer }
        totalPages: { type: integer }
        nextCursor: { type: string, nullable: true }
    Problem:
      type: object
      required: [type, title, status, code]
      properties:
        type: { type: string, format: uri, example: "about:blank" }
        title: { type: string }
        status: { type: integer }
        code: { type: string }
        detail: { type: string }
        instance: { type: string }
        errors: { type: array, items: { type: object } }
    Actor:
      type: object
      description: >
        Acting principal snapshot (API_CONVENTIONS §15, ADR-0011 §6). `principalType` is the
        write-time snapshot of `users.principal_type` — a transfer act may be performed by a
        HUMAN or an AGENT. `HUMAN` for every MVP transfer; the form is present so agents activate
        without a contract change (ADR-0006).
      required: [actorId, principalType]
      properties:
        actorId: { type: string, format: uuid }
        principalType: { type: string, enum: [HUMAN, AGENT] }
        actorDisplayName: { type: string, nullable: true }
    ClaimCodeRequest:
      type: object
      description: >
        Mint-claim-code request body (C5). Empty body → the code nominates the authenticated caller
        as the recipient user. `forOrganizationId` (optional) → the code instead nominates that org
        as recipient; the caller MUST be an authorized org-admin of it (else 403).
      properties:
        forOrganizationId:
          type: string
          format: uuid
          nullable: true
          description: Nominate this organization as recipient instead of the caller-as-user (caller must be its org-admin)
    ClaimCode:
      type: object
      description: >
        A minted transfer claim code (C5). `code` is returned in plaintext ONCE and is never
        retrievable again; the server stores only its lookup key. Opaque, high-entropy, single-use.
      required: [code, expiresAt, recipientType]
      properties:
        code:
          type: string
          description: Opaque single-use claim code (e.g. Crockford-base32, unguessable). Share out-of-band with the current owner.
          example: "T7Q4-9KJ2-M3XZ"
        expiresAt:
          type: string
          format: date-time
          description: When the code expires (now + 15m). After this it is uniformly invalid.
        recipientType:
          type: string
          enum: [USER, ORGANIZATION]
          description: Which side this code will resolve to on redemption (USER = the caller; ORGANIZATION = the nominated org)
    TransferInitiate:
      type: object
      description: >
        Initiate-transfer request body. The recipient is selected by **exactly one of** three
        mutually-exclusive selectors (C5): `claimCode` (recommended, recipient-minted opt-in),
        `toUserId`, or `toOrganizationId`. `toUserId`/`toOrganizationId` mirror the
        `ownership_transfers` exactly-one-of-user/org CHECK (ADR-0013 §3); `claimCode` resolves
        server-side to one of those two. Supplying zero selectors → 422 `RECIPIENT_REQUIRED`;
        supplying more than one → 422 `RECIPIENT_AMBIGUOUS`.
      required: []
      properties:
        claimCode:
          type: string
          nullable: true
          description: >
            A single-use transfer claim code the recipient minted via `POST /transfers/claim-codes`
            and shared out-of-band. Validated and consumed atomically; resolves to the recipient
            user or the org the recipient nominated. Invalid/expired/used/malformed → uniform 422
            `TRANSFER_CLAIM_CODE_INVALID` (no existence oracle). Mutually exclusive with
            toUserId/toOrganizationId.
        toUserId:
          type: string
          format: uuid
          nullable: true
          description: Recipient user by id (only for a userId already held from an in-system relationship; mutually exclusive with claimCode/toOrganizationId)
        toOrganizationId:
          type: string
          format: uuid
          nullable: true
          description: Recipient organization by id (mutually exclusive with claimCode/toUserId)
        transferReason:
          type: string
          nullable: true
          maxLength: 2000
          description: >
            Optional free-text reason the initiator gives; persisted on the transfer and copied
            to `animal_ownership_history.transferReason` on completion (ADR-0013 §3 delta).
      oneOf:
        - required: [claimCode]
        - required: [toUserId]
        - required: [toOrganizationId]
    Transfer:
      type: object
      description: >
        An ownership transfer (the process entity). MVP states: PENDING → {COMPLETED | CANCELLED}.
        `IN_PROGRESS`/`FAILED` are reserved for the Phase-2 verified flow (gated, absent in MVP).
      required:
        - id
        - animalId
        - status
        - initiatedBy
        - createdAt
        - updatedAt
        - expiresAt
      properties:
        id:
          type: string
          format: uuid
          description: Transfer ID
        animalId:
          type: string
          format: uuid
          description: Animal being transferred
        fromUserId:
          type: string
          format: uuid
          nullable: true
          description: From-side user (set iff the current owner at initiate was a user; exactly-one-of with fromOrganizationId)
        fromOrganizationId:
          type: string
          format: uuid
          nullable: true
          description: From-side organization (set iff the current owner at initiate was an org; exactly-one-of with fromUserId)
        toUserId:
          type: string
          format: uuid
          nullable: true
          description: Recipient user (exactly-one-of with toOrganizationId)
        toOrganizationId:
          type: string
          format: uuid
          nullable: true
          description: Recipient organization (exactly-one-of with toUserId)
        status:
          type: string
          enum: [PENDING, COMPLETED, CANCELLED]
          description: >
            MVP lifecycle state. PENDING = awaiting recipient; COMPLETED = accepted & re-attributed;
            CANCELLED = declined / cancelled-by-initiator / expired (see terminalReason).
        terminalReason:
          type: string
          nullable: true
          enum: [declined, cancelled_by_initiator, expired]
          description: >
            Why a CANCELLED transfer ended (maps to the DB `failure_reason` column, ADR-0013 §3).
            Null while PENDING and on COMPLETED.
        transferReason:
          type: string
          nullable: true
          description: Free-text reason the initiator supplied at initiate
        initiatedBy:
          allOf: [ { $ref: '#/components/schemas/Actor' } ]
          description: Principal that initiated the transfer (snapshot {actorId, principalType}, ADR-0011 §6)
        respondedBy:
          allOf: [ { $ref: '#/components/schemas/Actor' } ]
          nullable: true
          description: >
            Principal that accepted/declined the transfer (null while PENDING; set on COMPLETED
            via accept and on CANCELLED via decline; null when CANCELLED via initiator-cancel or
            expiry).
        expiresAt:
          type: string
          format: date-time
          description: >
            When a still-PENDING transfer is treated as expired (set to createdAt + 72h, OQ-2).
            Expiry is evaluated lazily on the next read/action (no background worker in MVP).
        completedAt:
          type: string
          format: date-time
          nullable: true
          description: When the transfer finalized to COMPLETED (null otherwise; distinct from updatedAt)
        createdAt:
          type: string
          format: date-time
          description: Initiation timestamp
        updatedAt:
          type: string
          format: date-time
          description: Last-update timestamp (backs the weak ETag)
  responses:
    Problem:
      description: RFC7807 problem (application/problem+json)
      content:
        application/problem+json:
          schema: { $ref: '#/components/schemas/Problem' }
