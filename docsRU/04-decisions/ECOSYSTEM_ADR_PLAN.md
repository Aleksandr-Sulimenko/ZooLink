# Ecosystem ADR Plan & Open-Decision Memo (architect) — для ратификации владельцем

**Status**: decision-memo (architect, 2026-07-01). Не ADR — бриф для быстрой ратификации.
**✅ Q1–Q6 ратифицированы владельцем 2026-07-01.** ADR 0014 + 0015 приняты совместно (Q1). **Обновление статусов (выровнено к файлам ADR 2026-07-02):** 0016 и 0019 **Accepted** 2026-07-01 (по sign-off security+legal); **0017 Accepted 2026-07-02** (go владельца на РФ-only топологию); **0018 остаётся Proposed** с явным условием-ожидания (см. таблицу и §Итог ратификации). doc-keeper зафиксировал статусы; ничего не закоммичено.
**Inputs**: `AUDIT_2026-06-30.md` (§Open owner/architect decisions Q1–Q6), `docsRU/01-discovery/future-features.md` §145-227 (vision Ecosystem Expansion).
**Companion ADRs** (статус как ратифицировано 2026-07-01):

| ADR | Title | Status | Суть одной строкой |
|---|---|---|---|
| **0014** | Offering supertype — polymorphic discovery+moderation seam | **Accepted** (2026-07-01, совместно с 0015) | Логический супертип Offering через полиморфный `(offering_type, offering_id)` над пер-подтиповыми таблицами; анти-god-table/анти-EAV; подтипы строятся только когда выпускается их сторона. |
| **0015** | `market_scope` refines ADR-0002 | **Accepted** (2026-07-01, совместно с 0014) | Жёсткий сплит остаётся для объявлений о животных (рынок выводится из вида); предложения-без-вида несут `market_scope ∈ {pet,livestock,both}`; discovery обеспечивает его. **Уточняет 0002, не supersedes.** |
| **0016** | Provider model | **Accepted** (2026-07-01) — матрица верификации T0–T3 + трёхрежимный иммунитет от security+legal; остаточные продуктовые OD-3/4/5 открыты | Одна абстракция Provider `provider_kind ∈ {ORG,INDIVIDUAL,AGENT}` над существующими принципалами; risk-tiered верификация = условие иммунитета посредника (legal). |
| **0017** | RF data residency | **Accepted** (2026-07-02) — go владельца на РФ-only топологию; devops реализует region-pin + fail-on-non-RF guardrail | ПДн граждан РФ — первичная+реплики+бэкапы+DR+объектное-хранилище остаются в РФ; трансгранично только для де-идентифицированных данных. **P0 go-live блокер** (legal A3) — закрыт на уровне решения. |
| **0018** | Cross-aggregate access rule | Proposed — готов (низкий риск); ожидает кивок владельца | Чтение животного через `AnimalService`; никаких сырых кросс-агрегатных чтений таблиц. **Подтверждает 0004**, предпосылка для 0014. |
| **0019** | PII-at-rest form enforcement | **Accepted** (2026-07-01) — owner OD-1/OD-2 + sign-off at-rest от security+legal; остаточное расследование сертифицированного СКЗИ | Построить blind-index+crypto-шов ADR-0012 сейчас (необратимый кусок); storage-level baseline на запуске; застейджить раскатку field-encryption. **Уточняет 0012, не supersedes.** |

> **ПОЧЕМУ эта памятка** — Шесть ADR выше отвечают на *структурные* вопросы. Шесть открытых решений (Q1–Q6) смешивают структурные, продуктовые, денежные, юридические и roadmap-вызовы. Эта памятка даёт позицию architect'а по каждому; владелец ратифицировал все шесть 2026-07-01 (см. §Итог ратификации), и каждый неструктурный вызов, помеченный **«owner decides»**, сохранён как зафиксированное решение.

---

## Итог ратификации (владелец, 2026-07-01)

Владелец рассмотрел Q1–Q6 и ратифицировал так. Этот штамп — авторитетная запись; пер-вопросные секции ниже сохраняют исходное обоснование architect'а без изменений.

- **Q1 — ратифицировано.** ADR-0014 + ADR-0015 приняты **совместно** (два связанных ADR, не один объединённый документ). Оба переведены Proposed → **Accepted**, каждый несёт «Ratified by owner 2026-07-01».
- **Q2 — ратифицировано.** Escrow **отложен**: никакого ADR-F сейчас; зарезервированы только не-денежные швы (`monetization_type`, деньги как integer minor units) через ADR-0014. Вернуться, когда `payments` появится next-up в roadmap.
- **Q3 — ратифицировано.** contact-exchange / mark-sold / views — **плановые, незапущенные слайсы (не регрессии)** → **P1 build** (эмитить value-события in-tx). Отслеживается, ничего не отброшено молча.
- **Q4 — ратифицировано.** Завести feature-toggle `goods_marketplace` **сейчас** (form-now, default off), независимо от ADR-0014. Handoff: backend сидит строку (крошечная миграция + схема + EN↔RU).
- **Q5 — ратифицировано.** Пер-рыночные значения SLA / listing-duration, определённые в **одном каноническом источнике** (де-дублицировать константу); `premium_profiles` **разделить на два** разных концепта (B2C consumer boost vs B2B subscription) с разным `monetization_type`. Сами пороги и тайминг запуска остаются **owner-decides** (product/finance/legal).
- **Q6 — ратифицировано.** Livestock B2B — **отдельный roadmap-трек**, та же кодовая база / те же швы (ADR-0014/0015/0016, `market_scope=livestock`), **секвенирован последним**. Выделенная команда/спринт = решение владельца по ресурсам.

**Открытые follow-up** (выведены на поверхность, не запаркованы молча; обновлено 2026-07-02): 0016 → **sign-off получен/Accepted**; остаточные продуктовые OD-3/4/5 отслеживаются в ADR. 0017 → **Accepted**; теперь **задача devops** (region-pin + fail-on-non-RF CI guardrail + фикс deployment-спеки — спека в ADR-0017 §Спецификация guardrail); остаётся legal review ст.12 carve-out. 0018 → всё ещё **Proposed**: кивок владельца (низкий риск, подтверждает 0004), затем ограниченный backend-рефакторинг (его судьба решается на слайсе D8 `marketOf`-рефактора). 0019 → **sign-off получен/Accepted**; остаточное расследование сертифицированного СКЗИ отслеживается в ADR.

---

## Q1 — ADR-A & ADR-B: один объединённый документ или два связанных? → **Два связанных ADR, ратифицированы совместно** (рекомендация architect'а, структурная)

**Рекомендация: два отдельных, кросс-связанных ADR (0014 + 0015), ратифицированных парой.**

Обоснование:
- **Разные цели и глаголы.** ADR-0015 — это **поправка к существующему Accepted-решению** (ADR-0002) — он должен чисто указывать на 0002 и уточнять его *scope*. ADR-0014 **вводит новый структурный концепт** (супертип Offering). Сворачивание поправки-к-0002 в документ нового супертипа замутняет цепочку supersede/amend и делает будущую кросс-ссылку («что уточнило 0002?») неоднозначной.
- **Прецедент.** Ровно так был структурирован ADR-0011 — *отдельный* документ, который «Amends ADR-0006», а не переписывает его. Держание поправки дискретной сохраняет audit trail.
- **Разделимое обоснование, связанная валидность.** 0014 (как discovery/moderation ссылаются на любое предложение) и 0015 (как разделение рынков выживает с предложениями-без-вида) имеют независимое обоснование, но ни один не *реализуем* без другого — поэтому они **ратифицируются совместно**, и каждый несёт кросс-ссылку «ratify jointly — see Q1».

Итог: ясность цепочки supersede + независимое обоснование выигрывают у удобства одного документа. **Два связанных ADR, совместная ратификация.**

---

## Q2 — Escrow: ADR-F form-now или полностью отложить? → **Отложить ADR; зарезервировать только дешёвые швы сейчас** (рекомендация architect'а; деньги/legal → owner decides по таймингу)

**Рекомендация: НЕ писать ADR-F (escrow / money-custody) сейчас; зарезервировать *не-денежные* швы через ADR-0014 и отложить решение по escrow до тех пор, пока `payments` не окажется на ближнем roadmap.**

Обоснование:
- Escrow = **удержание средств пользователей** → 115-ФЗ AML + 161-ФЗ payment-agent/banking лимиты + 54-ФЗ (legal C1/C2). Это самая тяжёлая compliance-нагрузка во всём vision'е и *последний* шаг монетизации (finance: включать `payments`/escrow только когда GMV покрывает 54-ФЗ/115-ФЗ/эквайринг; высокий чек livestock окупает это раньше pet).
- Form-now анти-rewrite швы, которые escrow'у понадобились бы, **уже зарезервированы без ADR escrow**: поле `monetization_type` (ADR-0014) и деньги-как-integer-minor-units (уже API-конвенция). Нет **необратимого куска схемы/контракта**, rewrite которого вынудила бы отсрочка escrow — поэтому §5 говорит *отложить*.
- Написание money-custody ADR сейчас запекло бы предположения (какой PSP, agent vs escrow модель, легальность custody), которые legal+finance не могут зафиксировать, пока модель не реальна → преждевременно.

**Owner decides** *тайминг* (когда `payments`/escrow входит в roadmap). Архитектурная позиция: **никакого ADR-F сейчас; пересмотреть, когда `payments` next-up.** Флаг: boost/premium это *собственные* B2C-услуги Оператора → 54-ФЗ ККТ применяется в момент взятия денег независимо от toggle `payments` (finance/legal) — этот фискальный рельс это *отдельный* триггер от escrow.

---

## Q3 — contact-exchange / mark-sold / views: плановые незапущенные слайсы или регрессия? → **Плановые-незапущенные (не регрессия); подтвердить с backend, затем приоритет P1** (позиция architect'а; backend подтверждает)

**Прочтение architect'а: это непостроенные слайсы, не регрессии** — таблица `contact_reveals` существует без writer'а/endpoint'а (зарезервированная форма), `sold_at`/SOLD-переход никогда не были подключены, у `views` нет источника данных. Этот паттерн (форма-схемы есть, поведение отсутствует) согласуется с дисциплиной проекта «форма-сейчас, поведение-позже», а не с чем-то, что работало и сломалось.

Следствие для severity (аудит спросил это, чтобы *выставить* severity): трактовать как **P1 функциональные пробелы**, не P0 инциденты. Но два из них — **value-события, от которых зависит North-star** (продажа, contact-reveal) — data-analyst отметил, что воронка слепа без них. Поэтому: **не блокер запуска, но высокий приоритет**, т.к. слой измерения и seller-notification оба зависят от них.

**Action**: backend подтверждает, что каждый был плановым слайсом (быстрая проверка плана/backlog); затем приоритезировать contact-exchange + owner-mark-sold + источник `views` в P1, эмитя соответствующие события in-tx (привязка к P1-пункту event-шва). **Owner decides** только если бизнес хочет формально отбросить любой из них (это нарушило бы «ничего не отбрасывается молча» — поэтому это надо трекать в любом случае).

---

## Q4 — toggle `goods_marketplace`: INSERT сейчас или ждать ADR-A? → **INSERT сейчас** (рекомендация architect'а, структурная)

**Рекомендация: зарегистрировать `goods_marketplace` в `feature_toggles` сейчас (form-now, default off), независимо от ADR-0014.**

Обоснование:
- Владелец явно назвал его требуемым toggle (vision §B монетизация: «Новый toggle к регистрации: `goods_marketplace`»). Отсутствие в `feature_toggles` — это **нарушение «ничего не отбрасывается молча»** (аудит GAP-BA-011).
- Строка feature-toggle — это дешевейшая возможная форма — идентична тому, как `payments`, `leasing`, `vet_leadgen` и т.д. уже засижены «форма есть, поведение отложено». Она несёт **ноль** зависимости от полиморфного шва ADR-0014; toggle просто резервирует имя/намерение.
- Ждать ADR-A значит смешивать тривиальную регистрацию со структурным дизайном — нет причин их связывать.

**Action**: backend добавляет seed-строку `('goods_marketplace', …, false, 0)` в той же форме, что другие gated toggle'ы (крошечная миграция + схема + EN↔RU). Не код architect'а — handoff к backend. **Решение architect'а: INSERT сейчас.**

---

## Q5 — SLA / duration: единый порог или пер-рыночный? + `premium_profiles` B2C vs B2B → **architect рекомендует пер-рыночный с единым каноническим источником; `premium_profiles` разделить** (owner decides финальные числа)

**SLA / listing-duration — рекомендация: пер-рыночные значения, но определённые в ОДНОМ каноническом источнике.**
- Аудит нашёл противоречия: SLA `24h` (`listing_state_machine.md`) vs `4h/6h` пер-рыночно (`12-moderation-domain.md`); duration `30` (SM) vs `60` (pet BR) vs `90` (livestock BR); плюс `SLA_TARGET_SECONDS` дублирован в 2 файлах. Дефект — **множественные источники**, а не сама пер-рыночная идея.
- ADR-0002 уже говорит, что два рынка эволюционируют независимо → **пер-рыночные SLA и пер-рыночная duration архитектурно корректны** (более высокая ценность и более медленный темп livestock реально отличаются от pet). Фикс — сделать так, чтобы пер-рыночные значения жили в **одном каноническом месте** (таблица спеки модерации/листинга), на которое ссылаются везде, с единым источником константы в коде — а не сплющивать их в одно число.
- **Owner decides** реальные пороги (4h vs 6h vs 24h; 60 vs 90 дней) — это product/ops-вызов. Архитектура: **пер-рыночно, единый канонический источник, де-дублицировать константу.**

**`premium_profiles` — рекомендация: разделить на два разных концепта.**
- finance+growth нашли, что `premium_profiles` смешивает **B2C consumer-фичу** (платный boost профиля) с **B2B-подпиской**. У них разное фискальное обращение (B2C → 54-ФЗ ККТ; B2B → счёт) и разный тайминг монетизации.
- Архитектура: моделировать их как **два отдельных offering'а/toggle'а** с разным `monetization_type` (ADR-0014) — не перегружать один toggle. **Owner decides**, запускать ли/когда каждый; finance владеет экономикой; legal владеет фискальным заключением.

---

## Q6 — Livestock B2B-трек: отдельная команда/спринт или roadmap-история? → **Отдельный roadmap-трек, не отдельная кодовая база; секвенировать последним** (рекомендация architect'а; ресурсы → owner decides)

**Рекомендация: livestock B2B — это *отдельный roadmap-трек/последовательность*, разделяющий ту же кодовую базу и швы ADR-0014/0015 — не отдельная команда и не форкнутая система.**

Обоснование:
- ADR-0002 уже жёстко разделяет два рынка *внутри одной платформы* (общее ядро: identity, сущность animal, инфра). Услуги/товары livestock B2B отличаются участниками и каналами (вет большого скота, оптовые корма, ВетИС, транспорт), но едут на **том же** шве Offering (ADR-0014) с `market_scope=livestock` (ADR-0015) и той же моделью Provider (ADR-0016). Нет структурной причины форкать.
- growth секвенировал его **последним** (Stage 4): pet услуги → pet товары → экспертиза → livestock B2B. Каждая категория — свой двусторонний cold-start; livestock B2B — отдельная channel-проблема, лучше открываемая после доказанной ликвидности pet.
- Получает ли он **выделенную команду/спринт** — это **решение по ресурсам, которое делает владелец** — архитектура его не требует и не запрещает. Архитектурная позиция: **одна кодовая база, один шов, отдельный roadmap-трек, секвенирован последним.**

---

## Handoff (что каждая роль делает дальше; ничего из этого не закоммичено)
- **owner**: ратифицировать ADR 0014–0019 (перевести `Proposed → Accepted` по вашему решению); решить Q2-тайминг, Q3 (если что-то отбрасывается), Q5-пороги + запуск `premium_profiles`, Q6-ресурсы; подписать at-rest launch-floor ADR-0019 с security+legal.
- **doc-keeper**: зеркалить ADR 0014–0019 + эту памятку в `docsRU/04-decisions/` (EN↔RU); добавить index-строки в оба README (EN сделал architect; проверить RU).
- **backend**: Q4 (seed `goods_marketplace`), Q3 (подтвердить плановые слайсы), рефакторинг ADR-0018, crypto-шов/blind-index ADR-0019 — всё по go владельца.
- **devops**: pinning региона ADR-0017 + CI guardrail + фикс deployment-спеки; storage-level шифрование ADR-0019.
- **security + legal**: risk-матрица верификации ADR-0016; sign-off at-rest launch-floor ADR-0019; review carve-out ст.12 ADR-0017.
- **alpha-analyst**: контракт полиморфных discovery+moderation (ADR-0014), контракт provider+верификации (ADR-0016), Gherkin фильтра `market_scope` (ADR-0015) — когда строится сторона.

---

## Wave D — план швов и упорядочивание (architect, 2026-07-04)

Чинит forward-compat находки AUDIT3: **циркулярность ADR-0018** (3 сайта джойна, CTE очереди недекомпозируем → разрешено split'ом из двух частей, см. ADR-0018 §Amendment 2026-07-04) и **тайминговое самопротиворечие ADR-0014** (разрешено ADR-0014 §Amendment 2026-07-04 rule 11). D1 (views-capture, миграция 0031) сделан. Миграции строго последовательны с **0032**.

### Шов → форма → D-слайс → миграция?
| Шов | Форма (колонка / таблица) | Default / gate | D-слайс | Миграция? |
|---|---|---|---|---|
| **OfferingRef** | `offering_type VARCHAR` + `offering_id UUID` на `favorites` **и** `saved_searches`; контракт `offeringType`/`offeringId`; словарь субъекта модерации реконсилируется позже | `offering_type` default+`CHECK IN ('ANIMAL_LISTING')` (аддитивно) — ведёт себя listing-only | **D2** | **0032** |
| **производный кэш `market`** | `listings.market VARCHAR(9) CHECK IN ('pet','livestock')`, вычисляется in-tx из `species.market`, бэкфилл, пересчёт на admin-исправлении вида | чистый read-кэш — без изменения поведения; **не** назначенный тег `market_scope` | **D3** | **0033** |
| **accessor AnimalService** | публичный `getOwnedAnimalForActor` + консолидация `isOrgAdmin` | code-only; 404-no-leak; паритет поведения | **D4** | нет |
| **субъект value-события** | payload события `offeringType`/`offeringId` на Sold/ContactReveal/view | `ANIMAL_LISTING`; воронка охватывает предложения | **D5** | нет |
| **мультироль** | junction `user_roles(user_id, role)`; `users.role` остаётся primary (ADR-0022) | спит; authz MVP читает `users.role` | **D6** | **0034** |
| **geo_anchor** | реконсилировать два near-me endpoint'а в один geo-контракт; зарезервировать `geo_anchor` как ключ discovery | point-now (lat/lng есть); PostGIS gated/отложен | **D7** | нет |
| **рефакторинг marketOf** | CTE очереди + обе `marketOf` читают `listings.market`; убрать все 3 джойна `animals⋈species`; флип ADR-0018 Accepted | grep-gate зелёный (0 сырых джойнов вне AnimalService) | **D8** | нет |
| **monetization_type** | spec-only резерв `{LEAD_GEN,SUBSCRIPTION,TAKE_RATE,NONE}` на контракте предложения | **SPEC-ONLY ГОТОВО** (ADR-0014 §Amendment 2026-07-05 (D9)); enum канонизирован SCREAMING_SNAKE, envelope read-model НЕ ДОЛЖЕН нести его сейчас, *модель* монетизации отложена до обсуждения с владельцем (win-win, soft-start), рождается с подтипом | **D9** | ✅ зарезервировано (только документация) |
| **market_scope (назначенный) + discovery read-model** | назначенный тег `{pet,livestock,both}` + материализованная проекционная таблица | **ОТЛОЖЕНО** до первого подтипа без вида; очередь/discovery используют кэш `listings.market` до тех пор | **D10** | отложено |
| **контроллер favorites** | строить против контракта OfferingRef из D2 | — | **D11** | нет |

### Исполнимый порядок (зависимости соблюдены)
1. **D2** — шов OfferingRef (0014 form-now, миграция **0032**) → разблокирует D11.
2. **D3** — производная колонка-кэш `market` (cycle-breaker 0018, миграция **0033**) → разблокирует list-path D8.
3. **D4** — accessor AnimalService + консолидация `isOrgAdmin` (0018 Part-1, code-only) — *истинная* предпосылка 0014.
4. **D5** — полиморфный субъект value-события (code-only).
5. **D6** — junction `user_roles` (ADR-0022, миграция **0034**).
6. **D7** — реконсиляция geo_anchor / near-me (контракт/code-only).
7. **D8** — рефакторинг marketOf завершён (code-only; нужны D3+D4); **флип ADR-0018 → Accepted сделан**; добавить CI grep-gate.
8. **D9** — spec-резерв `monetization_type` (alpha-analyst; doc-only). ✅ ГОТОВО (ADR-0014 §Amendment 2026-07-05 (D9)).
9. **D10** — discovery read-model + назначенный `market_scope`: **ОТЛОЖЕНО** до первого подтипа без вида (alpha-analyst пишет только заглушку контракта).
10. **D11** — сборка контроллера favorites (нужен D2) + тесты.

Потребляемые миграции: **0032** (D2), **0033** (D3), **0034** (D6). Всё прочее — код / контракт / отложено.

### Решения владельца к вынесению
1. **Модель мультироли (ADR-0022):** junction-with-primary vs `roles TEXT[]`; и политика self-claim (свободные нерегулируемые роли, регулируемые гейтятся тиром ADR-0016?).
2. **`market_scope` для предложений без вида:** подтвердить назначенный `{pet,livestock,both}` с `both` = одно предложение в обоих контекстах (ADR-0015), и сцепку с верификацией для регулируемых категорий.
3. **Форма `monetization_type`:** ✅ РЕШЕНО (D9, ADR-0014 §Amendment 2026-07-05) — enum = `{LEAD_GEN,SUBSCRIPTION,TAKE_RATE,NONE}` (каноническое SCREAMING_SNAKE); envelope read-model НЕ ДОЛЖЕН нести его сейчас — ждать подтип.
4. **Производный кэш `market` (D3):** подтвердить, что контролируемая денормализация (по-прежнему производна, просто кэширована) приемлема vs строгий always-join — она несёт производный `market`, а не назначенный `market_scope`, поэтому rule 7 ADR-0015 держится.
5. **Флип ADR-0018 → Accepted:** подтвердить флип 2026-07-04 (низкий риск, подтверждает 0004).

**Разрешено 2026-07-05 (владелец, «по рекомендациям»):** (1) мультироль = **junction-with-primary** (ADR-0022 OD-A) + self-claim = **свободные нерегулируемые роли, регулируемые гейтятся тиром ADR-0016** (OD-B) → **ADR-0022 Accepted**; (2) `market_scope` без вида = назначенный **`{pet,livestock,both}`**, `both` = одно предложение в обоих контекстах (ADR-0015) — **подтверждено**; (3) `monetization_type` = **SPEC-ONLY**, модель монетизации отложена до явного обсуждения с владельцем (win-win, soft-start) — не строится сейчас; (4) производный кэш `market` (D3) принят (по-прежнему производный, кэширован; не назначенный тег — rule 7 ADR-0015 держится); (5) **флип ADR-0018 → Accepted подтверждён**.
